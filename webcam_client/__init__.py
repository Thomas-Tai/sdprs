"""SDPRS Webcam Client package.

Streams USB webcam footage to the SDPRS Dashboard via outbound HTTPS
(1Hz JPEG snapshots + on-demand H.264 HLS). This package holds the client-side
modules (config, camera manager, push engine, control channel, GUI).
"""
