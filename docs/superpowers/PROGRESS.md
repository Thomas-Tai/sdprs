# SDPRS — Codebase Audit & Progress Ledger

**Originally audited:** 2026-07-13 (evidence-based — every claim re-verified against the working tree)
**Last updated:** 2026-07-19 — *after* the dashboard MEDIUM/LOW cleanup slice (entry 31; 9 commits closing the residual audit tier — hybrid dispatch after a session-cap boundary forced 4 files inline + 3 files via parallel Claude agents + 1 file inline pickup).
**Repo:** `sdprs/` (its own git repo; parent folder is not versioned) · **Remote:** `github.com/Thomas-Tai/sdprs`
**Current branch/commit:** `main` @ `29cff78` — local (push pending operator review).

**Related audits:** [Dashboard Audit 2026-07-15](dashboard_audit_2026_07_15.md) — 72 findings across 7 tranches (WS contract, bootstrap error boundary, snapshot z-order, snooze provenance, N+1, etc.). Rounds 1–3 (entries 22–24) addressed the vast majority; the residual is limited to hardware-blocked / decision-required items enumerated in §8.

> Canonical, living progress tracker for the `docs/superpowers` workstream.
> Task-by-task execution detail for the pump-merge effort lives in
> `.superpowers/sdd/progress.md` (SDD ledger). This file is the higher-altitude
> project-state view and the reconstruction-theme tracker.

---

## 0. Current status (headline)

**The pump-merge reconstruction + the entire V2 SPA are merged to `main` and pushed to origin.** `main` had been idle since 2026-05-09; it is now the live line again. The two security defects that rode in with the SPA baseline have been fixed. **The Theme-6 glass-hardening slice is pushed, and its detector-health telemetry is now wired end-to-end** — edge heartbeat → server `/api/nodes` → SPA — so a blinded-but-online camera shows 視覺/音訊 health + degraded status to operators. **Theme-4 data-lifecycle correctness (retention delimiter, pump_readings pruning, orphan-MP4 sweep, weather_config persistence) is pushed** (`dddda76`). **Theme-2 auth-hardening (UTF-8-safe constant-time credential compares, per-IP login throttle, hardened session-cookie flags, edge node_id allowlist, MQTT auth+ACL deploy template) is pushed** (`c4660d8`). **Theme-5 observability (dashboard request-storm coalescing, WS broadcast head-of-line-blocking fix, offline-mark TOCTOU false-alarm fix) is pushed** (`9d3f063`). **Theme-3 glass offline-autonomy (upload gives up on 4xx / after max retries instead of retrying forever, MP4-missing marks FAILED not false-UPLOADED, non-blocking MQTT connect at boot) is pushed** (`73b5af0`). **The dashboard's last per-refresh N+1 (`/pump/{id}/cycles` per pump) is collapsed to a single `/api/pumps/cycles` batch call** (`525452e`). **Glass MQTT Last-Will now marks a crashed node OFFLINE instantly** (`461c0ed`). **The non-blocking glass event-capture redesign is implemented and shipped DORMANT** behind `capture.async_encode` (default off) — a triggered event no longer stalls the capture loop with a 5s camera-blocking record + inline ffmpeg; post-frames come from the circular buffer and encoding runs on a worker thread (`34efdf6`, local-only). Flag stays off until the §8 hardware-validation gate. **The `datetime.utcnow()` deprecation debt is cleared** — all 32 production sites migrated to a naive-UTC helper (`3a61fef`). **The 2026-07-14 server slice (`e73718d`) makes the alert-response request paths PG-safe (Theme 1), puts auth on the snapshot GET, and pushes an instant `node_status: ONLINE` on node recovery** — the dead `broadcast_node_status` is deleted, and with LWT instant-offline both node transitions now push live. **The 1,986-line README is reconstructed into an audience-based 17-file `docs/` tree** (root front door + deployment/operations/reference + `PROJECT_STATUS.md` executive one-pager). **A user-requested full audit (duplicates / mode selection / redundancy / docs completeness) shipped its fix slice (`c0ff3b1`)**: the `get_node` route-shadowing 500s, the dormant async-encode drop-every-event contract break, the can-never-trigger fixed audio mode, the STORAGE_DIR/STORAGE_PATH retention split-brain, handover/audit PG-safety, and the justified duplicate-merges are all fixed. **315 tests pass** (edge_pump 55 · central_server 131 · edge_glass 129).

The remaining work is the (now-advancing) open reconstruction themes plus one **hardware** gate: the pump sensors are **not yet bench-commissioned**, so they ship OFF / analog-only until spec §6 is done. No coding blocker remains on what shipped.

**Dashboard-audit followup shipped 2026-07-15/16 as four commits** (`3a7b66e` bulk actions + session-expiry modal + N+1 pre-select + audit-CSV export + mobile nav + snapshot ticker consolidation; `4c4ffa0` snooze-provenance derived from `operator_actions` in one batch query (no schema migration); `0e3b3dc` `pages.jsx` split into 7 per-page files via a fresh GLM parallel session; `1291eb0` legacy Jinja dashboard fully retired — 3,483 LOC deleted, 5 routes → 301 → `/`, `alert_rate.intensifying` field finally dropped). A 14-finding adversarial code review ran between phases and closed 2 HIGH + 4 MED + 5 LOW issues before ship.

**Dashboard security + a11y sweep shipped 2026-07-16 as six commits** (`9a35809` nginx `/storage/` public alias removed — CRITICAL auth bypass; `d722cf2` auth fail-closed + random setup defaults — CRITICAL, `main.py` was silently swallowing the validator's ValueError; `8f9d7f2` 4-agent + 6-GLM SPA fleet + 8 a11y CRITICAL/HIGH fixes + C-8 NodeSidePanel history reactivity properly wired + exportAuditCsv Promise-reject fix; `e98a244` dashboard-guide keyboard-shortcut table + api.md refresh; `253b08f` unsnooze test coverage + audit provenance tests — 13 pass; `e04472e` WS whitelist contract test — guards server/SPA event drift). Storage + auth audits also completed inline — 12 findings delivered (1 CRITICAL path traversal, 5 MEDIUM, 6 LOW) awaiting a follow-up hardening commit. **328 tests passing; SPA remains the only dashboard front end.**

---

## 1. What happened 2026-07-13 (chronological)

1. **Full codebase audit** (this document's original content, preserved in §4–§8). Established that a large, review-clean body of work was stranded on the unmerged `spec/pump-merge` branch.
2. **Final whole-branch review — 9 parallel subagents** (3 review · 3 fix · 3 verify), scoped pump-firmware / server-backend / V2-SPA over `0ceeb22..0595060`:
   - **Biggest catch, from triage rather than the scoped reviewers:** `scripts/setup_esp32.sh`'s hardcoded on-device copy list was **missing `control_logic.py` and `sensors.py`** — the refactor's new required modules. The `-f` guard silently skipped them, so a provisioned node would **ImportError on boot**. Fixed (deploy script + README manual command + module tree); removed the dead `water_sensor.py`. — commit `6c099f0`
   - **PostgreSQL `/api/nodes` 500:** `snoozed_until` was passed raw to a Pydantic `Optional[str]` → `datetime` rejected → HTTP 500 for the whole list whenever a node was snoozed. Wrapped in `_ts_to_iso()` at both sites + REST regression test. — commit `65dc906`
   - **Dashboard:** `60/cycles` rendered "每 Infinity 分" for idle pumps; guarded both cards + added `role="alert"` to the sensor-conflict banner. — commit `a2a8bd0`
   - All three fix areas independently re-reviewed → **SAFE TO COMMIT** (incl. confirming the regression tests fail if the fix is reverted, and that the deploy list now covers every module `main.py` imports).
3. **Merged to `main`** via `git merge --no-ff spec/pump-merge` → merge commit `1c39b9e`. Re-verified 151 tests on the merged result.
4. **Theme-2 security slice — 2 parallel fixers:**
   - **SPA inline-`<script>` injection:** `dashboard_page` injected the username via `json.dumps(user)`, which does not neutralize a `</script>` breakout. Added `_js_safe_json()` (escapes `<`, `>`, `&`, U+2028/9). — commit `55b351a`
   - **Spoofable audit attribution:** single-alert-resolve trusted a client-sent `resolved_by` for the DB write, WS broadcast, and tamper-evident audit log. Now derives it from the authenticated session (matching bulk-resolve); SPA stops sending it. — commit `4a8bdc2`
5. **Pushed** `main` → `origin/main` @ `4a8bdc2`. Deleted the merged `spec/pump-merge` label. Final sweep: **155 tests pass**.
6. **Docs commit** `d7424bd` (this file rewritten to post-merge state) — **pushed** → `origin/main`.
7. **Theme-6 glass-detection hardening slice — 5 parallel subagents** (one per source file, file-disjoint, coding against frozen interfaces `AudioDetector.is_stale` / `VisualDetector.blinded` / `MQTTClient.set_detector_health`), then orchestrator-run full suite + runtime cross-file integration smoke test. — commit `22e6084` (local-only):
   - **Silent permanent blinding (headline T6 risk):** a sustained brightness shift (day/night, lights, camera move) made every frame an "anomaly" forever → visual detector dead with only a debug log. Now re-baselines after `>fps*3` consecutive anomalies and exposes a public `blinded` flag. Single-frame anomalies still return None (unchanged).
   - **Dead simulation path:** `--simulate` / MQTT `simulate_trigger` built the forced event in dead code *after* the trigger block → never encoded/enqueued (the only end-to-end node self-test was silently broken). Rewired to fire a processed event that bypasses cooldown.
   - **Stale-pairing correlation:** trigger fusion now requires BOTH detectors triggered within the window of *now* and resets timestamps after firing (a stale visual could previously pair with a fresh audio).
   - **Observability (also feeds T5):** heartbeat now carries `visual_health`/`audio_health` (telemetry-only) via `set_detector_health()`; main loop emits it + a throttled WARNING when the node is in a can-never-alert state (audio disabled/stale, visual paused/blinded).
   - **Reliability:** camera reopen re-applies resolution/fps (was silently degrading detection); real-time audio ring-buffer write vectorized (overrun→dropped-audio risk) + `is_stale()` liveness.
   - Tests: edge_glass **63 → 96** (new `test_trigger_engine`, `test_mqtt_heartbeat`, `test_main_helpers`, + audio/visual recovery & staleness). Full repo **188 pass**.
8. **Detector-health consume-side — 2 parallel subagents** (file-disjoint: backend Python vs SPA `.jsx`, coordinating via a frozen field contract), then orchestrator-run server suite + SPA review. — commit `3c110a2` (local-only). Closes the T6 observability loop: the edge was emitting `visual_health`/`audio_health` into the void.
   - **Server:** `mqtt_service._handle_heartbeat` stores the two fields (node_states + `metadata` JSON — **no migration**); `NodeStatus` + both `/api/nodes` construction sites expose them. Mirrors the existing `buffer_health` REST path. (`broadcast_node_status` is dead code → WS left untouched.)
   - **SPA:** `mapNode` normalizes + **folds detector health into node status** (online camera that is blinded/paused/disabled/stale → `warn`); new shared `<DetectorHealth>` atom renders 視覺/音訊 pills (camera-only) in NodeCard + NodeSidePanel; `detectorHealthMeta` labels (正常/已暫停/已致盲/訊號停滯/未啟用/未知).
   - Tests: central_server **44 → 47** (heartbeat stores fields; `/api/nodes` surfaces them incl. a blinded/disabled node). Full repo **191 pass**. SPA has no JS harness → verified by review.
9. **Theme-4 data-lifecycle — 2 parallel subagents** (file-disjoint: retention engine vs `weather_config`), then orchestrator-run server suite + review of the destructive orphan-sweep + delimiter regression test. — commit `dddda76` (local-only).
   - **Retention delimiter ~24h boundary bug:** `events.created_at` is stored space-delimited (SQLite `CURRENT_TIMESTAMP`) but was compared lexicographically against a `T`-delimited `isoformat()` cutoff → up to a day of boundary events mis-classified. Normalized both sides with SQLite `datetime()` in `retention_service.py` (SELECT+DELETE) and the two exported-but-unused `event_service.py` helpers.
   - **`pump_readings` never pruned** (unbounded on a 24/7 pump) → now pruned by `datetime(timestamp)`, guarded for older DBs.
   - **Orphaned MP4s** → fail-safe sweep (surviving-ref set built post-DELETE, path-normalized, mtime guard, any uncertainty keeps the file).
   - **`weather_config` wiped every PG startup** (`database.py` unconditional `UPDATE … NULL`) → removed; fresh installs still default empty; SQLite schema-guarded path untouched.
   - Tests: central_server **47 → 54** (delimiter boundary regression that fails on revert, pump prune + missing-table, orphan sweep, weather_config re-init persistence + PG-source guard). Full repo **198 pass**.
10. **Theme-2 auth-hardening — 5 parallel subagents** (file-disjoint: `auth.py` · `main.py` · `config.py` · `alerts.py`+`snapshots.py` · `deploy/`), coordinating via a frozen contract (4 new config settings + `auth.verify_node_id()` + `authenticate_user()`), then orchestrator-run full suite + review of the two highest-risk files. — commit `c4660d8` (local-only).
    - **Timing-safe credential compares:** every secret compare (`EDGE_API_KEY` ×3 sites, dashboard user+password) went through plain `==`/`!=`, leaking length/prefix timing. All routed through a new `_ct_equal()` that compares **UTF-8 bytes** via `secrets.compare_digest` — bytes form is deliberate: `compare_digest` on non-ASCII `str` raises `TypeError`, and this is a Traditional-Chinese deployment where `DASHBOARD_PASS` may be non-ASCII (would otherwise 500 a valid login). `authenticate_user` evaluates both fields unconditionally (no short-circuit) so timing can't reveal which was wrong.
    - **Login brute-force throttle:** per-client-IP monotonic-clock failure counter (`LOGIN_MAX_ATTEMPTS`/`LOGIN_LOCKOUT_SECONDS`); on lockout returns 429 **without** checking creds; success clears the counter. Per-process/in-memory (documented; fine for the single-worker MVP). Login now flows through the constant-time `authenticate_user` instead of an inline `==`.
    - **Session-cookie flags:** `same_site="lax"` (CSRF) + `https_only=COOKIE_SECURE` (default False keeps HTTP-LAN working; flip once TLS-fronted). `httponly` is Starlette's default.
    - **Edge node_id allowlist (trust boundary):** `verify_node_id()` gates `POST /api/alerts` (client-supplied node_id) and `POST /api/edge/{node_id}/snapshot` (path). `ALLOWED_NODE_IDS` empty by default ⇒ allow-all ⇒ **backward compatible** with the current single-node deploy; set it to lock ingest to known nodes. Video-upload path left alone (node_id there is server-derived, already trusted).
    - **MQTT broker hardening (deploy):** `mosquitto.conf` gains a loud secure-mode block (password_file + acl_file + `allow_anonymous false`, commented so the running anonymous-LAN MVP isn't broken); new `mosquitto_acl.conf` least-privilege template (server `readwrite sdprs/edge/#`; per-node write-own / read-cmd) + `MQTT_SECURITY.md` operator guide. **No secrets committed**; ACL topics grounded on `shared/mqtt_topics.py` but flagged as reconcile-before-enable.
    - **Test-isolation catch (integration):** `test_node_allowlist.py` initially set `ALLOWED_NODE_IDS` at *import* scope, which pytest's collection phase leaked into sibling modules (broke `test_config_auth_settings`'s default-assert + an alerts test). Moved the env mutation into a module-scoped setup/teardown fixture (cache-cleared both ends) → isolation restored. Production code was never at fault.
    - Tests: central_server **54 → 72** (+18: auth-hardening 6, login-throttle 4, config-settings 2, node-allowlist 6). Full repo **216 pass**.
11. **Theme-5 observability — 3 parallel subagents** (file-disjoint: SPA `app.jsx` · `websocket_service.py` · `mqtt_service.py`), then orchestrator-run full suite + close read of the SPA change (no JS harness → review-verified). — commit `9d3f063` (local-only).
    - **Dashboard request-storm (headline, deployed-today):** the SPA called a full `refreshLive()` (4 GETs + one `/cycles` per pump, N+1) on **every** inbound WS message, with no debounce and no overlap guard → an alert burst multiplied into a request storm against the 24/7 dashboard. Introduced a shared **in-flight guard** + **300ms trailing-debounce**: WS events and the 20s safety-poll now funnel through one guarded runner so `refreshLive()` never runs concurrently and a burst collapses to a single refresh; exactly one trailing run fires if events arrive mid-flight (no missed state). User-action `refresh()` (ack/resolve/snooze) stays forced + awaitable + post-mutation-fresh.
    - **WS broadcast head-of-line blocking:** `broadcast()` awaited `send_json` **serially** per client → one slow/stalled client delayed (or with backpressure, stalled) delivery to all others. Now sends concurrently via `asyncio.gather` with a **5s per-client `wait_for` timeout**; a timed-out/failed client is isolated and removed instead of blocking the pool.
    - **Offline-mark TOCTOU false alarm:** `_check_offline_nodes` selected stale nodes under the lock, released it, then `_mark_node_offline` set OFFLINE **unconditionally** — a heartbeat arriving in the gap produced a false OFFLINE (spurious pump CRITICAL logs, operator noise, WS churn), flipped back ONLINE on the next beat (flapping). `_mark_node_offline` now **re-validates staleness under the lock** before committing; a fresh heartbeat aborts the transition. (Confirmed all `last_heartbeat` writers hold `self._lock`, so the re-check is race-free.)
    - Tests: central_server **72 → 81** (+9: `test_ws_broadcast` 4 — slow/failing/timed-out client isolation; `test_offline_detection` 5 — genuine-offline + fresh-beat-in-gap for glass & pump + already-offline/removed guards). SPA verified by review. Full repo **225 pass**.
12. **Theme-3 glass offline-autonomy — 2 parallel subagents** (file-disjoint: `comms/api_uploader.py` · `comms/mqtt_client.py`), then orchestrator-run edge_glass suite + review of the uploader branch ordering. — commit `73b5af0` (local-only).
    - **Upload retried forever on 4xx (headline):** on a 4xx the worker returned WITHOUT `increment_retry` and WITHOUT changing status, so `get_pending()` re-served the row **every 1s forever** (no backoff, never drains) — pure log-spam + a queue that never empties. Now: 4xx (non-429) → terminal `FAILED` immediately (won't self-heal); 429 → treated as transient (backoff); 5xx / timeout / connect-error → backoff **and give up to `FAILED` after `MAX_RETRIES=10`** (there was no cap at all before). **Interacts with the T2 node_id allowlist:** a 403 for an unlisted node no longer spins endlessly. `FAILED` is excluded by `get_pending` so it's terminal with no schema change.
    - **MP4-missing falsely marked UPLOADED:** when the local clip was gone, the worker set status `UPLOADED` — signalling success though nothing uploaded (server alert stuck `PENDING_VIDEO`, clip silently lost). Now marks `FAILED` (honest terminal).
    - **Blocking MQTT connect at boot:** `start()` used the blocking `connect()` and **early-returned on failure before `loop_start()`** — a broker momentarily down at boot left MQTT permanently dead (paho's configured `reconnect_delay_set(1..60s)` auto-reconnect never engaged). Now uses non-blocking `connect_async()` and **always** starts the loop, so the node recovers automatically when the broker returns.
    - **Deliberately deferred (NOT rushed into this batch):** the blocking **MP4 encode** on the capture loop + **buffer arithmetic**. The correct fix (post-trigger frames from the circular buffer + off-thread encode) is coupled to the unconfirmed buffer sizing AND to camera-thread-safety (`record_post_trigger` reads the same `VideoCapture` as the main loop), and can't be validated without hardware — it needs a dedicated design pass, not an unvalidatable parallel edit.
    - Tests: edge_glass **96 → 107** (+11: `test_api_uploader` 8 — 4xx/429/5xx-max/mp4-missing/happy; `test_mqtt_client_start` 3 — connect_async-not-blocking, loop-starts-even-when-connect-raises, idempotent). Full repo **236 pass**.
13. **Dashboard `/cycles` N+1 → batch endpoint — 2 parallel subagents** (file-disjoint: `api/nodes.py` · SPA `api.jsx`), coordinating via a frozen response contract, then orchestrator-run server suite + SPA review. — commit `525452e` (local-only). This closes the last of the T5 request-fan-out remainder.
    - **Backend:** new `GET /api/pumps/cycles?window=1h` → `{window, nodes:{<id>:{count,alert}}}` for **every** pump node in one call. Extracted a shared `_count_pump_cycles()` helper + window map + alert threshold so the batch and the existing single `/pump/{id}/cycles` are provably identical (`test_batch_matches_single_endpoint`). Pump nodes identified by the DB `nodes.node_type` column (verified in `database.py`). Single endpoint kept for back-compat.
    - **SPA:** `loadNodes()` (runs on every refresh) now issues ONE `/api/pumps/cycles` call instead of one `/pump/{id}/cycles` per pump. Guarded (no pumps → 0 calls) + degrades to zeros on 404/error so an older server never breaks node loading. Every pump still gets a numeric `_cycles` for `mapNode`.
    - Tests: central_server **81 → 85** (+4: batch returns-all-pumps/glass-excluded, no-readings-zero, matches-single, alert-threshold). SPA review-verified. Full repo **240 pass**.
14. **Glass MQTT Last-Will (instant offline) — 2 parallel subagents** (file-disjoint: edge `comms/mqtt_client.py` · server `services/mqtt_service.py`), on a frozen LWT topic+payload contract, then orchestrator-run both suites + a test-isolation fix. — commit `461c0ed` (local-only).
    - **Edge:** `_init_client()` now calls `will_set()` (before connect) so the broker publishes `{node_id,status:OFFLINE,online:false}` to the node's OWN heartbeat topic (`sdprs/edge/{id}/heartbeat`, qos0, retain=False) when it drops ungracefully (crash / power-loss).
    - **Server:** `_handle_heartbeat` detects the `online:false`/`status:OFFLINE` marker and routes to a new `_handle_lwt_offline()` that forces OFFLINE **unconditionally** (unlike the timeout path's `_mark_node_offline`, it does NOT re-validate staleness — an LWT means the node is definitively gone), mirroring the DB-update + `node_status` WS broadcast. Normal heartbeats still mark ONLINE. **No new subscription** (reuses the existing `sdprs/edge/+/heartbeat`). This is an optimization on top of the ~90s heartbeat-timeout fallback, which remains.
    - **Test-isolation fix (mine):** the edge LWT test patched `comms.mqtt_client.mqtt.Client`, but paho isn't installed in this env so module-level `mqtt` is `None` → `AttributeError`. Replaced the whole `mqtt` name with a `SimpleNamespace(Client=Fake)` so `_init_client` runs against a fake factory. Production code was never at fault.
    - Tests: central_server **85 → 89** (+4: LWT marks offline / forces offline despite recent beat / normal-beat-still-online / unknown-node-no-crash); edge_glass **107 → 109** (+2: will_set topic/payload/qos/retain, set at construction before connect). Full repo **246 pass**.
15. **Non-blocking glass event-capture — 2 parallel subagents** (file-disjoint: NEW `utils/event_capture.py` · `edge_glass_main.py`+`config.yaml`), on a frozen module interface, then orchestrator-run edge_glass suite + a runtime cross-file integration smoke test + close review of the main-loop wiring. — commit `34efdf6` (local-only). Implements `docs/superpowers/specs/2026-07-13-glass-mp4-encode-buffer-redesign.md` with the recommended §10 defaults, **shipped dormant** behind `capture.async_encode` (default **false**).
    - **The bug:** on a trigger, the capture loop ran `record_post_trigger` (~5s, blocking, reading the *same* `VideoCapture` as the loop) + inline `encode_mp4` (ffmpeg, seconds) — during which the loop stopped appending to the buffer, detecting, fusing (a **second glass-break in that window was silently missed**), and pushing snapshots.
    - **The fix (async path):** a trigger just registers a `PendingEvent`; the loop keeps running; once `post_roll` elapses the loop freezes + `slice_window`s `[T-pre, T+post]` from the circular buffer and hands it to an off-thread `EncodeWorker` (bounded queue, drop-newest+WARN, drain-on-stop, encode-fail drop-after-log). Post-frames come from the buffer, so **`record_post_trigger`'s second camera read is gone** — the camera is read from one thread only. `clamp_capture_window` makes the buffer arithmetic an asserted invariant (`pre+post+margin ≤ duration`, clamp+WARN). Defaults: buffer 10s / pre 4s / post 5s (Option A — keeps the ~396 MB budget).
    - **Rollout:** `async_encode=false` is byte-for-byte the legacy blocking path (verified by diff review); the new path ships dormant until the spec §8 hardware-validation gate (bench Pi + camera) flips it on. `record_post_trigger` retained for the legacy path.
    - **Verification:** the new module is fully unit-tested; the main-loop wiring (not unit-testable) is diff-reviewed + a runtime smoke test replicated main()'s exact async sequence (clamp→add→due→slice→submit→enqueue) against the real module.
    - Tests: edge_glass **109 → 124** (+15: tracker due-boundaries, slice_window, clamp invariant, EncodeWorker enqueue/encode-fail-survives/drop-newest). Full repo **261 pass**.
16. **`datetime.utcnow()` deprecation migration — 3 parallel subagents** (file-disjoint: `services/*` · `api/*` · root `database.py`+`main.py`), consuming a helper I created first (`central_server/timeutil.py`), then orchestrator-run suite + a broad aliased-call sweep + a fix. — commit `3a61fef` (local-only). Clears the tracked debt before a Python bump (`utcnow()` is deprecated 3.12+, slated for removal).
    - **The trap:** `datetime.utcnow()` returns **naive** UTC; the codebase stores/compares naive-UTC timestamps and relies on `.isoformat()` having no tz suffix (retention delimiter logic, `last_heartbeat` math). A naive swap to `datetime.now(timezone.utc)` would add `+00:00` and silently break those paths — so the helper deliberately returns naive (`.replace(tzinfo=None)`), and `test_timeutil` locks that contract in.
    - All **32** production sites across 9 files migrated to `timeutil.utcnow()`. Pure refactor, zero behavior change (the pre-existing suite stayed green). Caught an aliased `_dt.utcnow()` in alerts.py that the name-based grep missed. Deprecation warnings **91 → 35** (remainder = test-file `utcnow` + Pydantic). Test files' `utcnow` left as-is (cosmetic, out of scope).
    - Tests: central_server **89 → 92** (+3 timeutil naive-UTC contract guards). Full repo **264 pass**.
17. **Full verification audit (2026-07-13)** — independently re-verified every *shipped* claim in this doc against ground truth (git sync, 264-test run, per-fix symbol presence in production code, dead-code/negative/security claims, ESP32 deploy list, allowlist backward-compat). **All shipped features confirmed present + green.** One discrepancy found + fixed: **`edge_pump/config.py` shipped `FLOAT_ENABLED=True`/`RAIN_ENABLED=True`** while this doc claimed sensors "ship OFF / analog-only until commissioned" — an un-commissioned node could feed a miswired (valid-but-wrong) digital reading into `control_logic.decide()`. Defaulted both to **False** (`71ec41a`) so a fresh node runs analog-only until §6 bench polarity-verify; `sensors.py` None-degradation means zero logic change. (Also confirmed: `broadcast_node_status` genuinely dead, snapshot GET genuinely public [documented-open], no leaked WiFi pw / public broker in tracked non-doc files, 0 production `datetime.utcnow()`.) *(Both the dead method and the public GET were then closed by `e73718d`, entry 18.)*
18. **PG-safe server slice — 3 parallel subagents** (built 2026-07-13, committed 2026-07-14 as `e73718d`; file-disjoint: data-layer+consumers · snapshot-auth · mqtt/ws), frozen interfaces, orchestrator-run authoritative suite + riskiest-diff review:
    - **T1 data access:** new `database.get_backend()` accessor + dual-backend `list_events`/`get_event_counts`/`acknowledge_event`/`resolve_event` (db arg now optional, alert args keyword-only) + 3 dashboard helpers (`get_pending_alert_ids`, `get_handover_note_row`, `get_event_created_ats`). All 6 `get_db()` bypass sites in `main.py`/`api/alerts.py` unified — **no production request path calls `get_db()` under PG anymore** (remaining calls = SQLite-branch internals in `database.py` + backend-guarded fallbacks in `event_service.py`).
    - **Bonus catch — latent `alert_rate` NameError:** `_dt.fromisoformat` ×2 with `_dt` unbound (the utcnow migration `3a61fef` removed the alias with the last `_dt.utcnow()`, missing these two uses) → any `/api/alerts/rate` call with non-empty buckets 500'd. Fixed to `datetime.fromisoformat`; the new dispatch tests close the coverage gap that hid it.
    - **Snapshot GET auth:** `GET /api/edge/{node_id}/snapshot/latest` now `Depends(verify_api_key_or_session)` (was public — LAN camera-privacy gap). Same-origin `<img>` carries the session cookie automatically, so the dashboard/monitor wall is unaffected. Dead `is_public_path`/`is_api_key_path` carve-outs removed.
    - **Instant-ONLINE push:** `_handle_heartbeat`/`_handle_pump_status` capture `prev_status` under lock and emit a transition-only `node_status: ONLINE` via `broadcast_from_sync`; dead async `broadcast_node_status` **deleted** (the long-standing "dead code" fact is retired). With LWT instant-offline (`461c0ed`), node up AND down now both push live. All WS consumers verified tolerant (base.html has an explicit ONLINE branch; monitor.js merges; SPA ignores unknown types).
    - Tests: central_server **92 → 115** (+23: dual-backend dispatch, snapshot-auth incl. a real session-login fixture, 7 online-recovery broadcast). Full repo **287 pass** — verified twice (pre-pause 2026-07-13, pre-commit re-run 2026-07-14).
19. **Docs reconstruction — 4 parallel subagents + PM pass (2026-07-14):** the 1,986-line zh-TW README decomposed into an audience-based tree — root `README.md` (121-line front door), `docs/README.md` (role-based index), `docs/architecture.md` + `docs/hardware-network.md`, `docs/deployment/` (7 guides + chooser), `docs/operations/` (dashboard-guide / runbook / troubleshooting), `docs/reference/` (configuration / api / mqtt-topics), `docs/PROJECT_STATUS.md` (executive one-pager, orchestrator-written), `docs/archive/` (Zeabur migration report `git mv`'d in). **Coverage audit:** all 21 source sections (一–二十 + 八A) land in exactly one new doc; 48/48 key command lines byte-exact; every relative link verified resolving. `docs/superpowers/` deliberately untouched. Old README recoverable via `git show e73718d:README.md`.
20. **Full-codebase audit + fix slice (2026-07-14, user-requested: duplicates/mode-selection/redundancy/README) — 4 read-only audit subagents (orchestrator-verified 6/6 top claims) → 3 file-disjoint fix subagents (central_server · edge_glass · edge_pump+shared), orchestrator-run suite + riskiest-diff review.** — commit `c0ff3b1`, **pushed**. All three fix agents (and one audit agent) were killed mid-edit by session usage limits and resumed via SendMessage with verify-before-edit instructions — the partial-edit hazard materialized and was handled.
    - **`get_node` route shadowing (headline server bug):** `api/nodes.py`'s `async def get_node` route rebound the module global, so the same-named DB import resolved to the route at 3 later call sites → guaranteed TypeError/500 on `GET /nodes/{id}`, `PATCH /nodes/{id}` (location), `POST /nodes/{id}/snooze` — all SPA-called; tests only covered the list route. Fixed via `get_node as db_get_node` + 3 regression tests that fail on unfixed code. (Bug class worth remembering: FastAPI decorators return the function unchanged, so a route named like an import silently shadows it.)
    - **Dormant `async_encode` path would have dropped every event:** `slice_window` returned bare ndarrays but `encode_mp4` sorts by `x[0]` and unpacks `(ts, frame)` → every async encode would raise and be swallowed; would have shipped broken at the §8 hardware gate. Fixed to preserve tuples + an encoder-contract test replaying `encode_mp4`'s exact frame ops + spec doc corrected.
    - **Mode-selection fixes:** audio `fixed` mode could never trigger (`fixed_db_threshold: 90` on a dBFS scale where values are ≤ ~0 → now **-30 dBFS** across detector/DEFAULTS/config.yaml with scale comments); `AudioResult` now carries real `db_peak`/`freq_peak_hz` (alert forensics were always 0.0 via `getattr` defaults); simulation events are marked `is_simulation` (telemetry-only), bypass AND don't consume the real-alert cooldown (a drill no longer blinds the node for 30s).
    - **Storage split-brain:** retention orphan-sweep watched `STORAGE_DIR` while uploads write `settings.STORAGE_PATH` → deployed sweep watched the wrong directory. Unified on `STORAGE_PATH` (legacy env honored + deprecation warning); `SECRET_KEY`/`DB_PATH`/`RETENTION_DAYS` now flow through validated Settings; `setup_server.sh` writes `STORAGE_PATH`.
    - **PG-safety completions (T1 tail):** handover GET/PUT + dashboard context now share dual-backend `set_handover_note`/`get_effective_handover_note` (3 drifted TTL copies deleted; PG no longer 500s); `audit_service` gained `_pg_log_action_sync`/`_pg_list_actions_sync` (audit trail was silently swallowed under PG); `get_event_created_ats` same-day SQLite window fixed with `datetime()` normalization (alert-rate sparkline returned zero rows for same-day cutoffs).
    - **Dedup merges (the "merge duplicates" ask):** alerts list/detail row-mapping unified in `_row_to_alert_detail` (the drifted list copy silently dropped `acknowledged_by/at` — live SPA bug); uploader JSON/video retry ladders → one `_send` (byte-identical logs/branches); handover TTL ×3 → one helper; pump clock shims ×2 → one; server cmd topics → `shared.topic_cmd()` with the silent import-fallback topic copies **removed** (hard import, valid in all three deploy layouts). **Deliberately NOT merged:** dual-backend `_pg_*` mirrors (18/20 pairs verified identical — the idiom, not drift), MicroPython pure functions (RAM + desktop-testability), the flag-gated legacy/async capture dual path.
    - **edge_pump wiring:** pull resistors now follow sensor polarity (blanket `PULL_UP` made a broken *active-high* high-water line read asserted → pump-ON on a wiring fault); `WIFI_CONNECT_TIMEOUT`/`SOCKET_TIMEOUT_S` actually consumed (dead `WIFI_MAX_RETRIES` deleted); battery pins ship `None` until bench day (also fixes pin-0 falsy guard); `build_config()`-vs-`DEFAULT_CONFIG` drift-guard test added.
    - **Orchestrator fix:** `AudioResult.triggered` carried a numpy bool despite the `bool` dataclass declaration (new tests caught it via `is True`) — cast at construction.
    - Tests: **287 → 315** (edge_pump 48→55 · central_server 115→131 · edge_glass 124→129), zero failures. Audit's phase 2 (dead-code pruning + docs corrections incl. credential redaction) dispatched as a follow-up slice (entry 21).
21. **Audit follow-up slice — dead-code pruning + docs completeness (2026-07-14, phase 2 of user's full-audit request) — 2 background agents + 4 supplementary parallel agents; verified-before-delete + tests re-green:** commit pending (ready). Two rounds of session-limit crashes; six SendMessage resumes; the "resume-with-verify" idiom paid for itself.
    - **Dead-code pruning (P2-A, `a1ab90248a062f0d1`):** every deletion candidate re-Grep'd across the tree first (SPA `.jsx`, templates, shell scripts all count as callers). Removed: `central_server/auth.py` × 5 (`get_current_user_optional`/`verify_ws_session`/`is_public_path`/`is_api_key_path`/`get_auth_context` + `starlette.websockets.WebSocket` import); `central_server/database.py` `connect_pg`/`disconnect_pg` (never-called async wrappers around `create_engine`); `services/event_service.py` × 6 (`get_event`/`create_event`/`update_event_video`/`delete_event`/`get_events_for_retention`/`delete_events_before_date` + unused `from datetime import datetime`); `services/weather_service.py` × 2 (`_parse_station_observation`/`_parse_township_forecast`; **kept** `_parse_typhoon_warning` — live via `_tick`, and `update_weather_location` — live via `api/weather.py:70`); `services/websocket_service.py` × 4 (`broadcast_new_alert`/`broadcast_alert_updated`/`broadcast_alert_resolved`/`broadcast_pump_status`; **kept** `broadcast_alert_acknowledged` — live in alerts.py:391, and `broadcast_from_sync` — 5 sites in mqtt_service.py); `services/retention_service.py` `get_storage_stats`; `services/__init__.py` re-exports pruned to match; `edge_glass/comms/mqtt_client.py` `is_connected`; `edge_glass/stream/rtsp_server.py` `StreamManager.is_active` + its `__main__` demo call; `edge_glass/comms/api_uploader.py` `_delete_local_mp4` + the 2 commented invocation lines. **Test files: zero deleted** — every deletion was of a symbol whose test-file also exercised OTHER symbols, so full files stayed. Endpoints/routes deliberately untouched (product surface). 315/315 tests still pass — same as `c0ff3b1`, proving zero-caller status was real.
    - **Docs completeness (P2-B `ad27ae60446849363` + 4 supplementary agents `ae3e25f013924c614` api.md · `a90949807b19554fa` configuration.md · `acaa4cb86835ced39` mqtt-topics.md · `a1281a453641c77d8` architecture+misc):** `docs/reference/api.md` **13 → 40 routes** grouped into 9 sections (health, session, alerts, snapshots, nodes+pump, stream, weather, handover/audit, dashboard pages, WebSocket) with auth column derived from the actual `Depends(...)` on each handler; `docs/reference/configuration.md` full rewrite covering all 27 Settings env vars incl. `ALLOWED_NODE_IDS`/`CWA_API_KEY`/`LOGIN_MAX_ATTEMPTS`/`MEDIAMTX_METRICS_URL`, `STORAGE_PATH` canonical vs `STORAGE_DIR` deprecated note, edge_glass `capture:` block + `snapshot:` + `visual.anomaly_recovery_seconds`, edge_pump `WIFI_CONNECT_TIMEOUT`/`SOCKET_TIMEOUT_S` + `*_ACTIVE_LOW` polarity warning (pull-resistor+reading tied together since `c0ff3b1`) + `FLOAT_ENABLED`/`RAIN_ENABLED=False` default, `SDPRS_SSH_USER`/`SDPRS_SERVER_HOST`/`SDPRS_GLASS_HOST`/`SDPRS_GLASS_NODES` deploy vars; `docs/reference/mqtt-topics.md` gains payload tables for `heartbeat` (9 fields incl. LWT `online:false`, `visual_health`/`audio_health`), `pump_status` (12 fields with all `reason` codes + optional `battery_voltage`/`power_source`), `stream_status`, `cmd/stream_*`, `cmd/snooze`, `cmd/simulate_trigger` + a mosquitto_pub演練 how-to; `docs/operations/dashboard-guide.md` rewritten for the V2 SPA at `/` (was documenting legacy Jinja); `docs/operations/troubleshooting.md` §5 falsely instructed operators to `mosquitto_pub cmd/*` at ESP32 (which is publish-only — no `subscribe()` in `edge_pump/mqtt_client.py`) → replaced with correct diagnostics (verify pump_status telemetry, dashboard, ESP32 REPL, physical power-cycle); §4 WebSocket note updated to V2 SPA's auto-reconnect UX; `docs/architecture.md` file tree regenerated from disk (adds `central_server/api/{audit,handover,weather}.py`, `services/{audit_service,weather_service}.py`, SPA `.jsx` tree, `timeutil.py`, `event_capture.py`, `edge_glass/config.zeabur.yaml`, `firmware/`, `deploy/emqx/`, `storage/`); `docs/deployment/README.md` gains a `scripts/` index; `edge_glass/config.zeabur.yaml` gets the same audit-slice corrections as `config.yaml` (was already partly correct — verified idempotent).
    - **Credential redaction (`Msc@2333` / `MSC-Person`):** redacted in `docs/superpowers/plans/2026-07-10-pump-merge.md` + `docs/superpowers/specs/2026-07-10-pump-merge-and-reconstruction-design.md`; two straggler mentions I found in `.superpowers/sdd/task-{5,6}-report.md` (historical audit prose reciting the leaked strings) also redacted to `Msc@***`. Post-fix grep across `sdprs/` returns **zero hits** for both patterns.
    - **Flagged, NOT auto-deleted:** `scripts/micropython_esp32.bin` (1,691,664 bytes, MD5 `356b3a28…`) — different size and content vs `firmware/micropython_esp32.bin` (1,702,240 bytes, MD5 `75bf224b…`); `setup_esp32.sh` reads from `firmware/` only. Older MicroPython build hanging on in `scripts/`. Not a straight duplicate → requires user decision before `rm`.
    - **Agent survival notes (durable):** the docs consolidation agent (P2-B) hit the 2:40pm Taipei session limit twice, the mqtt-topics agent hit "Connection closed mid-response" twice; the P2-A pruning agent also died once on the limit. All 6 resume-with-Read-first rounds landed cleanly. Splitting the monolithic P2-B into 4 file-disjoint focused agents (Sonnet, not Opus) drained the remaining work in ~20 min wall-clock after the second crash. **Lesson: dispatch narrower, smaller agents for docs consolidation — one big prompt is a session-limit magnet.**
    - Tests: **315 pass** (edge_pump 55 · central_server 131 · edge_glass 129) — unchanged from `c0ff3b1`. Pruning was **zero-behavior-change**: no test count moved, no test failed, confirming every deletion's zero-caller claim was real.
22. **Dashboard audit round 1–3 followup (2026-07-15/16, `3a7b66e`) — 12 parallel-agent slices across 3 rounds + 14-finding adversarial review, all shipped in one commit:** the 72-finding [dashboard audit](dashboard_audit_2026_07_15.md) was worked through as file-disjoint parallel-agent rounds coordinating on frozen contracts. Highlights of what actually landed:
    - **New bulk-action endpoints** — `POST /api/alerts/bulk-ack` + `bulk-resolve` (single UPDATE, per-id WS broadcast via new `database.get_events_by_ids`, `note` COALESCE, audit trail via new `ACTION_BULK_ACKNOWLEDGE`); SPA switched from N-round-trip to one call.
    - **Session-expiry modal** — server closes WS with `{type:"auth_expired"}` + close-1008; SPA's `_WS_EVENT_TYPES` whitelist accepts it; `app.jsx` halts reconnect and renders a 逾時 modal that redirects to `/login?next=...`. (Missing whitelist entry was the code-review's HIGH #1 — one-line fix.)
    - **Audit CSV export** — new `GET /audit/export.csv` (admin-only, session-auth, UTF-8 BOM, 10,000-row hard cap, no-store cache); SPA button wired.
    - **N+1 killers** — `get_events_by_statuses` (single query for list_alerts multi-status), `get_pump_readings_multi` (batches pump readings for `/pumps/cycles`).
    - **SPA polish** — mobile nav (hamburger + backdrop), volume persistence + `Howler.volume()` sync, shift-banner "查看交接" navigation, shared 1s ticker across all `SnapshotImage` tiles (one interval, not N), `safeSevMeta`/`safeStateMeta`/`safeDetectorHealthMeta` exposed on `window` and adopted at risky render sites, `AlertsPage.inDateRange` implemented (today/7d filter now works), `meOnly` filter no longer falls back to hardcoded `alice`.
    - **Adversarial review found 14 more (2 HIGH + 4 MED + 5 LOW)**, all fixed inline before ship: bulk pre-select was fetching top-N-of-status (silently dropped older selected rows from broadcast) → changed to WHERE id = ANY; `BulkAck/ResolveRequest.ids` was `list[str]` while SPA sent ints → widened to `list[Union[int, str]]`; bulk handlers now call an `onRefresh` prop threaded through from `app.jsx` so React state updates same-tick instead of waiting 20s for the poll.
    - Tests: 131 central_server unchanged; SPA has no JS test harness so review-verified. `test_pump_cycles_batch.py` monkeypatch migrated from `get_pump_readings` → `get_pump_readings_multi`.
23. **Snooze provenance from operator_actions (2026-07-16, `4c4ffa0`) — closes audit MED #4 without a schema migration:** `NodeStatus.snoozed_by` / `snoozed_at` had been shipping as `None` for weeks because the columns don't exist on the `nodes` table (memory-vs-reality gap discovered during code review). Rather than migrate, added `audit_service.get_snooze_provenance(node_ids)` → one batch SELECT of most-recent `ACTION_SNOOZE` per target_id (SQLite: expanded IN placeholders; PG: `= ANY(:ids)`). `list_nodes` calls it once with all `db_nodes.keys()` and populates both fields at every construction site. The MuteDrawer "由 X 於 HH:MM 設定" chip is now live. SPA already null-guarded, so stale provenance on since-unsnoozed nodes doesn't visibly leak. **Pattern durable:** operator_actions is the append-only source-of-truth for per-node audit-derived state; prefer batch-lookup helpers there over adding columns to `nodes`.
24. **GLM cross-model collaboration — pages.jsx split (2026-07-16, `0e3b3dc`):** a fresh GLM session ran in parallel, given a handoff prompt (see `scratchpad/glm_handoff_prompt.md`) covering environment quirks (`[Cloud]` path, `/c/Python314/python`, pytest-per-suite trap), SPA architecture (no build step, `window.*` cross-file plumbing), backend contracts (WS event whitelist, naive-UTC datetime rule, MQTT topics), and security constraints (redacted creds, banned untracked docs). GLM split `pages.jsx` (1,789 LOC) into 7 per-page files under `pages/` (alerts/monitor/status/weather/handover/audit/pumps), each exporting via `Object.assign(window, { PageName })`; `FilterChip` (used by 3 pages) promoted to `components.jsx`; `index.html` updated with 7 new `<script>` tags in correct order (components → pages/* → app). Net −8 LOC, zero behavior change, 131 tests still pass. **Two sessions on same filesystem = shared `.git`, so GLM's commit landed directly on my local `main`; I re-verified sizes/exports/pytest before pushing.** Working pattern: hand the collaborator a self-contained brief covering the traps that would otherwise take them a full session to discover.
25. **Legacy Jinja dashboard retirement (2026-07-16, `1291eb0`) — 3,483 LOC deleted, 5 routes → 301:** the V2 SPA at `/` has been the primary UI since May and covers every page the Jinja dashboard did (alerts/monitor/status/audit) plus new pages (pumps/weather/handover). Survey confirmed zero external systems reference the legacy routes: edge nodes hit `/api/*` not the pages; no email/Slack/push integration sends `/alerts/{id}` links. Deleted: `templates/base.html` (895), `dashboard.html` (248), `alert_detail.html` (358), `monitor.html` (174), `system_status.html` (427), `audit.html` (65), `static/js/dashboard.js` (571), `static/js/monitor.js` (821). `main.py`: replaced 5 Jinja routes with 301 redirects to `/` (courtesy for lingering bookmarks); deleted `_get_dashboard_context` helper (5 callers all gone); pruned 10 unused imports. **Only Jinja template left is `login.html`** (SPA can't render its own login). Finally dropped `alert_rate.intensifying` (audit LOW #14) — its only consumer was `base.html:682`, now gone; SPA re-derives from bucket counts. Docs updated: `api.md` (collapsed 5 legacy rows into one 301 line), `architecture.md` (tech table + file tree), `dashboard-guide.md` (retirement note). **131 tests unchanged.**
26. **Dashboard security-hardening + UX+a11y sweep + WS contract test (2026-07-16, 5 commits `9a35809`→`e04472e`) — parallel-agent orchestration across Claude subagents + 9 GLM handoff sessions, then Claude took over all remaining work when the user closed the GLM sessions:**
    - **`9a35809 security(nginx)`** — CRITICAL. `deploy/nginx.conf` exposed `location /storage/` as an anonymous alias to the raw MP4 storage tree (`Cache-Control: public, immutable`, `CORS: *`). Anyone guessing `node_id`+timestamp could pull event recordings without auth. Block removed; docker-compose volume mount dropped; `scripts/smoke_storage_auth.sh` added as regression check.
    - **`d722cf2 security(auth)`** — CRITICAL. Prior default setup wrote `changeme-strong-password` + `changeme-session-secret` and the fail-closed validator was silently downgraded to a warning in `main.py`. Combined = full auth bypass on any un-rotated deployment. Strengthened `validate_settings` (rejects KNOWN_INSECURE_VALUES incl. all `changeme-*` strings, any substring containing `changeme`, secrets <32 chars, <8 unique chars, passwords <8 chars). Removed the `main.py` try/except so startup now fails closed. `setup_server.sh` rewritten to generate `openssl rand`-based credentials; initial dashboard password written to `/root/sdprs_credentials.INITIAL.txt` (0600). Added `central_server/tests/conftest.py` so the 14 pre-existing test files' `test-*` placeholders (which now fail validation) become no-ops with strong shared defaults. New `MIGRATION.md` guides operators through rotation (SECRET_KEY invalidates cookies; EDGE_API_KEY forces edge re-flash). Test coverage: +10 SECURITY validation cases (12/12 pass).
    - **`8f9d7f2 feat(spa)`** — the parallel dashboard-audit-2026-07-15 remediation: 4 Claude subagents (pages/*, api/data helpers, components/StatusStrip, mobile-flow) + 6 GLM sessions (GLM-1..GLM-4, GLM-6). Layered on top: a11y CRITICAL/HIGH from a GLM-7 15-finding audit. Toast + NewAlertBanner now announce via a dedicated `sr-only-live` region with proper `role`/`aria-live` per tone. SnoozeMenu extracted from AlertDetail into a WAI-ARIA menu pattern (roving tabindex, ↑/↓/Home/End/Escape, click-outside-close, focus returns to trigger). StatusStrip toggles (focus-mode, mute, theme) got `aria-pressed` + descriptive labels + ring outlines so state isn't color-only. FilterChip `aria-pressed`. Countdown ♪Xs `aria-hidden`. Textarea `aria-label`s on resolve-note + handover-note. **C-8 fixed properly:** NodeSidePanel's `history` prop was declared in components.jsx but the caller (app.jsx) never passed it → panel silently fell back to non-reactive `window.NODE_HISTORY`. Hoisted history into React state, mirrored on refresh()/loadInitial(). **exportAuditCsv Promise-reject fix:** the fire-and-forget anchor click resolved successfully even on 403 (browser silently downloaded the JSON error body as .csv). Now HEAD-preflights, throws on non-2xx → pages/audit.jsx's catch block finally fires. 12 files, +962/−210.
    - **`e98a244 docs(operations)`** — dashboard-guide.md corrected: keyboard-shortcut table said `1`–`6` but code routes `1`–`7` (audit was on `7` since it shipped). Documented `7` swallowed inside resolve-template flow. Refreshed api.md endpoint surface (GLM-4).
    - **`253b08f test(backend)`** — GLM-3 unsnooze coverage: +8 `test_nodes_api.py` cases (DELETE snooze auth / idempotent / audit trail / provenance survives) + new `test_audit_service.py` (4 provenance cases). 13 pass.
    - **`e04472e test(ws)`** — WS whitelist contract test. Sweeps every `broadcast(...) / broadcast_from_sync(...) / websocket.send_json(...)` payload for `"type": "<literal>"` and asserts the union matches both `EXPECTED_ALL_TYPES` and the SPA `_WS_EVENT_TYPES` set. 3 pass. Guards against future silent drift (server adds a type, SPA ignores it → feature silently broken).
    - **Post-audit findings not yet fixed** (delivered inline in scratchpad, tracked for a follow-up commit): **Storage B** (CRITICAL path traversal — `verify_node_id` returns early when `ALLOWED_NODE_IDS==""`; hostile edge with the shared key can write `mp4_path` outside the events dir); **Storage D/E/F/G** (nosniff header, EXIF strip, retention hardcoded subdir, log-injection consistency); **Auth E-1** (CSRF Origin/Referer defense-in-depth); **Auth H-1** (login.html loads tailwind from external CDN — script has full page context before form submit); **Auth I-1/I-2** (failed login + lockout events not persisted to audit table). Highest-signal follow-up: bundle Tailwind locally and add path-traversal char check to `verify_node_id` (both single-line fixes).
    - **Working pattern captured:** file-disjoint parallel agents against frozen contracts + verify-before-edit resumes are the productive way to work through a 100+ item dashboard audit. The GLM sessions carried their own briefs (env quirks, security constraints, contracts); Claude coordinated. When the user closed the GLMs mid-sweep, the remaining work was picked up inline without re-planning (auth hotfix → SPA integration → docs → tests → audit deliveries → WS contract). Six commits shipped in one working session; zero test regressions.
27. **Storage + auth follow-up hardening (2026-07-16, 5 commits `0ae519e`→`45656f7`) — closes the six findings deferred from entry 26; 5-agent file-disjoint parallel dispatch, one agent died mid-run and was completed inline:** the entry-26 "post-audit findings not yet fixed" list (Storage B/D/E/F/G + Auth E-1/H-1/I-1/I-2) was worked as five file-disjoint parallel Claude subagents against frozen contracts, then integrated as five separate `security(...)`/`feat(retention)` commits. One agent (Auth-E1/I1/I2) hit an API-level connection drop mid-write with only the test file unfinished — its production code (`main.py` CSRFOriginMiddleware + `audit_service.py` constants + both `log_action` call sites in `/login`) had already landed cleanly on disk, so recovery was a Read/verify → write-the-missing-test inline, not a re-dispatch. **Zero rework**, all four production hunks kept.
    - **`0ae519e security(auth)` — Storage-B / Storage-G (Auth-G1):** `verify_node_id` now always applies a `[A-Za-z0-9._-]{1,64}` char-class check regardless of `ALLOWED_NODE_IDS` state, so a hostile edge holding the shared key can no longer walk `mp4_path`/`storage_path` outside the events tree via `..` or an absolute path — closes the CRITICAL path-traversal deferred from entry 26. `verify_api_key` on a rejected key now logs the first 16 hex chars of SHA-256(api_key) instead of the raw first 8 chars — operators can still correlate repeat offenders by digest without the log itself being a partial-credential leak. Tests: **12/12** (3 legacy + 6 char-class + 2 allowlist + 1 log-digest).
    - **`605c7a0 security(snapshots)` — Storage-D / Storage-E:** `receive_snapshot` now Pillow-round-trips the JPEG (`save(quality=88, optimize=False)`) before storing, deleting any GPS/EXIF that would otherwise expose device location to anyone who could pull a snapshot from the dashboard. Guarded fallback returns original bytes on ImportError or Pillow decode failure so a broken Pillow install does not break snapshot ingest (logged at DEBUG). `get_latest_snapshot` sets `X-Content-Type-Options: nosniff` on both live-snapshot and placeholder response paths, blocking a JPEG-with-embedded-HTML polyglot from being mis-sniffed as HTML by a browser. **Regression fix bundled in this commit:** three byte-equality assertions in `test_snapshot_api.py::TestGetSnapshot` (after_post, overwrite, different_nodes) can no longer hold under the Pillow re-encode. Rewritten to assert on `X-Snapshot-Status`, JPEG magic (`\xff\xd8`/`\xff\xd9`), and `X-Snapshot-Timestamp` advancement for the overwrite case; the strip contract itself is covered in the new `test_snapshot_hardening.py`. Tests: **5/5** hardening + **15/15** snapshot_api still green.
    - **`c504433 feat(retention)` — Storage-F:** `run_retention_cleanup` now accepts an optional `subdirs` list (defaults to `STORAGE_RETENTION_SUBDIRS` env, defaulting to `"events"` so existing deployments keep current behavior). Orphan-sweep and empty-dir cleanup loop over each configured subdir, so a deployment that stores snapshots or audit exports in a sibling of `events/` can now GC them under the same retention policy instead of manually cronning a `find` sweep. `STORAGE_RETENTION_SUBDIRS` added to BOTH the pydantic `BaseSettings` block and the dataclass fallback in `config.py`. **Contract preserved:** `pump_readings` prune, DB event delete, `surviving_refs`, `setup_retention_scheduler`, and return-dict schema all unchanged; `main.py` not touched (existing zero-arg callers keep working). Tests: **13** new (6 parser unit + 7 integration) + **11/11** existing retention suite green (backward-compat confirmed).
    - **`f6fe273 security(login)` — Auth-H1:** `/login` no longer pulls Tailwind from `cdn.tailwindcss.com`. The CDN load was a supply-chain risk (a CDN compromise could inject JS running BEFORE the user submits credentials, so the login page was strictly less trusted than any post-auth page). Vendored the same `tailwind.min.js` (451,131 bytes, byte-perfect copy of the SPA's own vendored copy) under `central_server/static/vendor/` and referenced via the local `/static/` mount. Added a Content-Security-Policy meta tag scoped to the login page: `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; form-action 'self'; base-uri 'self'; frame-ancestors 'none'`. `'unsafe-inline'` on `style-src` is required for Tailwind's runtime utility generator; `base-uri` and `frame-ancestors` lock down two subtler login-phishing vectors (unauth `<base>` tag + clickjacking iframe). Tests: **6/6** (HTML has no CDN, HTML references local tailwind, CSP meta present, vendored asset non-trivial size, GET /login serves local file, static mount serves the vendored asset).
    - **`45656f7 security(auth)` — Auth-E1 / Auth-I1 / Auth-I2:** `CSRFOriginMiddleware` guards `POST/PUT/PATCH/DELETE` on `/api/*` and `/logout` by comparing `Origin` (or `Referer`, fallback) against the request's own `Host` header — defense-in-depth backup to the `SessionMiddleware SameSite=Lax` cookie flag, catching same-site subdomain XSS forging cross-site POSTs, browser bugs leaking the cookie cross-site, and future misconfigs that flip `SameSite=None`. Bypasses safe methods, `POST /login` (unauthenticated form POST is the point), and requests carrying NEITHER `Origin` NOR `Referer` (curl/httpx/same-origin server-rendered forms — blanket-rejecting would break too many real clients). Uses `urlsplit()` rather than string-match so a fake scheme prefix or embedded allowed-host in path does not slip through. `CSRF_TRUSTED_ORIGINS` env (comma-separated) extends the allowlist, read at request-time (no restart needed). Added AFTER `SessionMiddleware` in code order → OUTER wrapper → CSRF check runs before any route dependency fires. **Audit persistence:** a failed `/login` now appends `ACTION_LOGIN_FAILED` (operator=attempted username, target_id=source IP); an attempt against an already-locked IP appends `ACTION_LOGIN_LOCKED` — deliberately once per attempt-while-locked, so operators see the row-pile grow when a bot is hammering and can identify the source IP without grepping across app logs. Tests: **10/10** (8 CSRF middleware + 2 audit persistence). **Existing `test_login_throttle.py` 4/4 still green** — the CSRF gate does not perturb the login flow because `/login` is on the bypass list.
    - **Agent-survival note (durable):** Agent-5 (Auth-E1/I1/I2) died with "Connection closed mid-response" during Step 3 (test file write), but Steps 1 (audit_service constants) and 2 (main.py CSRF + audit calls) had already landed cleanly on disk. Recovery was a `git status --short` + `git diff` inspection → confirmed production code intact → wrote the missing test file inline. **Lesson: on API-error mid-run, verify disk state before re-dispatch — production changes often survive a connection drop even when the agent reports failure.** First-run test failure exposed one small drift: TestClient's default base URL is `http://testserver` (not `http://testclient` as the summary claimed) — fixed the fixture and 10/10 passed.
    - **Working pattern captured:** the entry-26 "not yet fixed" list was structured precisely so entry 27 could be a 5-agent parallel sweep. Each finding got its own agent with a frozen scope (file list + test file to create) — no coordination overhead, no shared state, integration was 5 sequential commits from clean pre-staged diffs. **41 new tests + 3 regression fixes** shipped; total central_server test count now **131 → 172** (backward-compat pending a full sweep, but per-suite runs all green).
    - **Banned untracked paths (`docs/sdpr_UI_V2.zip`, `docs/sdpr_UI_V2/`, `docs/ui_redesign_v2_prompt.md`) remained untracked throughout — verified via `git status --short` before each of the 5 commits.**
28. **Dashboard UI/UX interaction audit + top-tier fix sweep (2026-07-16, 6 commits `bafc796`→`0d8f865`) — 64-finding audit + 14-item fix slice; 5 file-disjoint fix agents (2 Claude + 2 GLM + 1 Claude-inline after GLM stalled):** a 5-parallel-agent read-only sweep (dispatched via `/dispatching-parallel-agents`) covering the 5 disjoint SPA slabs (`alerts`, `monitor/status/pumps`, `weather/handover/audit`, `app/tweaks-panel`, `components/api/data`) returned **64 findings** ranked BLOCKER→LOW; the top tier (2B + 6C + 5 alerts-cluster HIGH + 1 tweaks-panel MEDIUM = 14 items) was fixed in one working session and shipped as 6 file-scoped commits with per-scope stories. All banned untracked paths (`docs/sdpr_UI_V2.zip`, `docs/sdpr_UI_V2/`, `docs/ui_redesign_v2_prompt.md`) remained untracked throughout — verified via `git status --short` before each commit.
    - **`bafc796 docs(audit)`** — 64-finding audit report at `docs/superpowers/dashboard_audit_2026_07_16_ui_interaction.md`. Distribution: 2 BLOCKER · 6 CRITICAL · 22 HIGH · 21 MEDIUM · 13 LOW. Includes an agent-scope table, cross-cutting patterns (bulk-select filter leak, state-not-reset-on-selection, save-race idiom, silent-truncation of exports, missing optimistic-lock, no-focus-return after destructive actions), all BLOCKER/CRITICAL detailed, HIGH clustered by area, MEDIUM/LOW as one-liners. Follow-up-slice pointer back-filled at the bottom of the doc with the 5 fix commits from this same entry.
    - **`7c1a74c fix(spa)` — handover.jsx (B1+B2):** shift-summary metric cells now use `s.alertsHandled ?? '—'` with a `const s = window.SHIFT_SUMMARY || {}` fallback, and the handover history render guards on `window.HANDOVER?.history?.length` before mapping (empty state otherwise). Uses `??` (not `||`) so a legitimate `0` count still renders as 0. Fixes a hard crash ("cannot read length of undefined") on any pre-load / 403 path AND the "undefined" cells on partial-load.
    - **`97e0072 fix(spa)` — alerts.jsx state-leak cluster (C1 + A1..A5):** six related fixes in one commit — (C1) useEffect keyed on `filtered` intersects the bulk-select `checked` Set with currently-visible IDs so a filter/tab change cannot resolve invisible alerts through the bulk button; (A1+A3) useEffect on `selectedId` closes the snooze menu and resets the resolve note draft on selection change so state authored for alert A does not bleed into alert B; (A2) `applyTemplate()` no longer unconditionally clears `noteEdited`, which used to erase the "edited" mark and re-apply the template on the next render; (A4) new `busy` state + `guardedSnooze(id, minutes)` wraps the Ack/Resolve/Snooze dispatch with `disabled={busy}` on the buttons, preventing double-fire from rapid clicks; (A5) `<AlertDetail key={selected.id} …>` remounts the detail on selection change — idiomatic React reset for a component that edits per-item state. **Follow-up flagged (task):** keyboard shortcuts `A/R/S` in `app.jsx` dispatch the same actions without going through the busy guard.
    - **`2b9c848 fix(audit)` — CSV mirrors on-screen filters + shift window (C2+C3) + latent SQLite `since=` format fix:** `/api/audit/export.csv` accepted only `type`, so compliance receiving the export got every SNOOZE row across all operators/history instead of the "today · my actions · SNOOZE" the operator saw (C2); separately, the 本班·我的動作 button filtered ONLY on operator with no shift/time window, returning every action the operator had ever taken and double-counting across shifts in the handover-note builder (C3). Backend gained `operator` + `since_ms` params (Unix ms → naive-UTC via `datetime.fromtimestamp(..).replace(tzinfo=None)`, matching the timeutil.utcnow contract), frontend composes both from the on-screen filter state (meOnly → self + 12h; otherwise operatorFilter + date-chip translated to local midnight or now-7d), and the meOnly button gained a "(近12h)" badge + tooltip so operators know the window is applied. `SHIFT_WINDOW_MS = 12h` constant chosen because SDPRS operator rotations run 12h. **Latent bug found + fixed en route:** `list_actions`'s SQLite branch passed `since.isoformat()` (T-delimiter) against `CURRENT_TIMESTAMP` rows (space-delimiter); lexical comparison silently dropped every row, so ANY future caller of `since=` would have gotten zero results. Now normalized via `.strftime("%Y-%m-%d %H:%M:%S")` and covered by a new regression test. Tests: **5/5** audit_service (incl. new since-format guard) + **10/10** auth persistence still green.
    - **`4f058dc fix(spa)` — tweaks-panel localStorage + origin gate (C4 + MED):** theme/density/wallMode/muted never persisted across reloads — `useTweaks()` read DEFAULTS only and the postMessage-to-parent persistence path was dead code in production (SDPRS is not embedded under the artifact host). Now `setTweak` writes to `localStorage['sdprs.tweaks']` as an atomic JSON object (matching the existing `sdprs.volume` naming convention in `app.jsx`) BEFORE echoing to postMessage/tweakchange; `useState` uses a lazy initializer that JSON.parses the stored object and shallow-merges over defaults. try/catch on both read + write handles Safari private-mode, corrupted storage, quota exhaustion. Companion MEDIUM fix bundled: the incoming-message listener now checks `e.origin === window.location.origin` before acting on `__activate_edit_mode`/`__deactivate_edit_mode`, so a parent frame, opener, or browser extension can no longer force edit mode. Production SDPRS is not cross-origin-embedded, so this does not regress the dev-host flow.
    - **`0d8f865 fix(spa)` — weather nulls + node-panel save race (C5+C6):** `window.WEATHER` initial state seeded numeric zeros (wind.speed=0, wind.gust=0, rain.now=0, temp=0, humidity=0) despite `api.jsx` explicitly warning "do NOT default gust to 0 — reads as 'no gust during a typhoon'". Any panel that renders these without gating on `available` would show a calm-weather snapshot during a real event. All numeric placeholders now `null`; audit of the 6 weather-consuming call-sites (StatusStrip, MuteDrawer, `app.jsx` typhoon fallback, WeatherPage, alerts, handover) confirmed each already uses either the `available` gate or optional chaining, so no reader-side patches were needed. **C6 (save race):** `NodeSidePanel.saveEdits` set `editing=false` BEFORE the server responded, silently reverting a failed PATCH with no visible error. Now async: awaits `onUpdateNode`, leaves edit mode only on success, sets `locationError='儲存失敗，請重試'` on failure. New `saving` state disables Cancel + Save during the in-flight request and shows aria-busy + '儲存中…' on the Save button (reset to false in the node-change useEffect so a panel switch mid-flight clears the badge). Companion `app.jsx onUpdateNode`: removed the try/catch that swallowed API errors with a toast + early return; errors now propagate to `NodeSidePanel.saveEdits` where they render as the inline error above. The success-only "配置已更新" toast survives.
    - **Agent-survival note (durable):** third fix agent (audit.jsx C2+C3) was dispatched to GLM and stalled with no output. Rather than restart with a fresh brief, killed GLM and picked it up inline — file-scoped enough (audit.jsx + api.jsx + one backend endpoint + one test) that Claude-native was as fast as another GLM round-trip. **Lesson: when a handed-off agent stalls, evaluate the scope before re-dispatching — small file-scoped work is often cheaper to complete inline than to re-brief.** Same recovery philosophy as entry-27's Auth-5 partial-completion (verify disk state first; keep what survived).
    - **Working pattern captured (extends entry-24 GLM cross-model collab + entry-27 file-disjoint parallel dispatch):** ship-tier hardening is now a repeatable loop — (1) parallel read-only audit sweep, (2) audit doc committed first so subsequent fix commits can reference finding IDs, (3) file-scoped fix agents dispatched in parallel with a mix of Claude + GLM (user directs the handoff based on complexity × review-load tradeoff), (4) integration reviewer (Claude) spot-checks each diff against its `failure_scenario` before commit. Interruptions are cheap because per-file isolation means one agent stalling never blocks the others; recovery is a two-line "kill GLM, take inline" decision when the scope is small.

29. **Dashboard UI/UX audit HIGH-slice follow-up (2026-07-16, 6 fix commits + 1 security commit `390622d`→`8ac7d67`; keyboard busy-guard hoist `9721e9c` first) — closes 22 of the remaining HIGH-severity findings from the 64-item interaction audit; 6-agent all-Claude parallel dispatch (user directive: "all claude" after entry-28's GLM stall):** entry 28 landed the 14 top-tier fixes; entry 29 completes the remaining HIGH bucket via a fresh audit doc re-read → file-disjoint scope carve → parallel Claude dispatch. All six agents returned clean diffs; integration was diff-verified against each finding's `failure_scenario` before commit. One agent (session/auth) surfaced an open-redirect gap on the backend that the SPA carrier would have otherwise exposed — the fix crossed the tier boundary and shipped as a single security-toned commit spanning both halves.
    - **`9721e9c fix(spa)` — hoist alert-action busy guard to App:** entry 28 landed `busy` state inside `AlertDetail` (commit `97e0072`, item A4), but the app-level keyboard shortcut listener called `onAck(...)` / `onResolve(...)` directly, bypassing the guard entirely. Rapid keyboard tapping could double-dispatch the same action. Fix hoists `alertActionBusy` into `App`, exposes it via prop to `AlertDetail`, and gates the keyboard branch (`case 'a'` / `case 'r'`) with the same check. Both entry-points now share one guard. Preserved as its own commit because it closes an explicitly-flagged follow-up from entry 28 (see the "keyboard shortcuts `A/R/S` in `app.jsx` dispatch the same actions without going through the busy guard" note).
    - **`390622d fix(spa)` — handover draft rescue + weather null bars + audit CSV busy-guard (Data-render H-2/H-3/H-4):** three data-render edge cases. Handover H-2/H-3: persist dirty text to `localStorage['sdprs.handover.draft']` on every keystroke + `beforeunload` prompt so tab close / browser crash mid-shift no longer destroys a long narrative; surface 24h TTL as an amber "即將過期 (Nh)" badge under 4h and a red "1 小時內過期" badge under 1h so the incoming operator is never blindsided by a note that vanishes minutes into their shift. Badge is only rendered when on-screen text equals `HANDOVER.current`, so a just-typed draft never inherits the previous note's stale age. Weather H-4: filter nulls before `Math.max` on wind/rain — the entry-28 C5 backend fix now emits `null` for future hours whose data hasn't landed, and a single `NaN` was poisoning the axis (`NaNpx` heights → blank chart); per-bar `hasRain`/`hasWind` checks render a dashed 4px stub + "—" so absent data reads distinctly from a calm-weather zero. Audit H-4: disable + `aria-busy` the CSV export button while `exportState.tone === 'info'` so a stressed operator on slow network can't queue 3-4 concurrent generations, downloads, and audit_export rows by mashing Enter.
    - **`0912d4f fix(spa)` — status.jsx per-row snooze busy-guard + pumps.jsx offline-first tone (Buttons-lie H-1/H-2):** two "buttons that lie about state" findings. Status H-1: extract the inline snooze button into `SnoozeRowButton` with per-row local `busy` state so a slow-VPN double-tap can no longer double-fire the API + double-toast for the same row; `onKeyDown` stops Enter/Space propagation so keyboard-driving the button doesn't also bubble to the surrounding row's Enter handler (which would open the side panel). Pumps H-2: classify pump state offline-first — old code let a stale-but-high last-known `level` render as a healthy card ("正常, 92%") when telemetry had stopped days ago, giving operators a false "high but OK" reading; new order is offline → critical (status OR `level > 85`) → warn (status OR `level > 70`) → ok. Offline cards render in `sev-stale` tone with a "離線" label so the visual state matches the trust state of the number underneath.
    - **`5b2f69a fix(spa)` — alerts.jsx ghost selection + SnoozeMenu key-bubble + a11y nesting (Selection H-1/H-2/H-3):** three alerts-page selection/bubbling fixes. H-1: intersect `selectedId` with the visible `filtered` list via `useEffect` so a severity chip flip, tab change, or search query no longer leaves the detail panel + keyboard A/R targeting a row that has scrolled out of view; empty filter falls back to null (clean panel). Same shape as entry-28's C1 `checked` intersection pattern. H-2: `stopPropagation` on Arrow/Home/End in `SnoozeMenu` keydown — `app.jsx` ArrowUp/ArrowDown also flips `selectedId` on the alerts page, and without this, using arrows to navigate the snooze menu silently reselected the underlying alert row. H-3: restructure `AlertRow` so the checkbox is a **sibling** of the `role="button"` region, not a child — nesting an interactive element inside `role="button"` collapses screen-reader announcements to just "button" and hides the checkbox from the a11y tree; `aria-label` added on the checkbox for row identification.
    - **`163e399 security(auth)` — `/login?next=` open-redirect defense + SPA session state carrier (Session/auth H-1..H-4):** end-to-end fix — Agent 3 flagged that app.jsx's `?next=<pathname>` was a no-op because `/login` hardcoded `RedirectResponse(url="/")`; fix crosses tier boundary. **SPA side:** session-expiry modal now base64-encodes `{page, selectedId, hadDraft}` into `?sdprs_state=<b64>` and appends it to the post-login target URL so the redirect back carries operator context all the way into the fresh app mount; a module-level `RESTORED_STATE` IIFE decodes on script load, seeds `useState` initializers for `page` and `selectedId`, and `history.replaceState`s the query param out so a reload doesn't re-apply stale state (toast on mount: `登入前的草稿未保存` if `hadDraft` else `已回復先前頁面`). `resolveNote` is intentionally NOT preserved (XSS + size). Global keydown handler gains an IME composition guard (`e.isComposing || e.keyCode === 229` — Bopomofo/Zhuyin first-stroke was firing shortcuts mid-word), extends `inField` with `SELECT` and `e.target.isContentEditable`, and bails Cmd/Ctrl+K when `sessionExpired` so palette-driven actions don't 401-flood the dead session. **Backend side:** `_safe_next_path()` in `main.py` rejects any absolute URL, protocol-relative (`//evil.com`), backslash-normalisation prefix (`/\evil.com`), or non-empty scheme/netloc via `urlparse`; anything suspicious falls back to `/`. GET `/login` preserves `next` into template context, `login.html` emits it as a hidden input via `{% if next %}`, POST reads + sanitises + honors it on the 303, and lockout + failed-login template responses thread `next` back through so retries preserve the target. Regression suite: 12 new cases in `test_auth_persistence_and_csrf.py` — hidden input echo present/absent, safe path honored, 7 parametrized attack shapes rejected, dirty-form retry preservation. **22 passed** (10 pre-existing + 12 new).
    - **`a6046e6 fix(spa)` — api.jsx FastAPI-detail surfacing + AbortSignal.any + openSocket contract + monitor.jsx 1Hz tick (API/WS H-1..H-4):** API-layer cluster. H-1: `apiFetch` non-2xx branch reads the response body (JSON via `.json().catch → .detail`, else `.text()`) so FastAPI's structured errors reach the operator's toast ("已被 alice 於 14:32 標記已解決" on a 409 race) instead of the opaque "HTTP 409 on /api/…" default; attaches `.detail`, `.body`, `.status` to the error for programmatic callers. **Behavior change flagged:** 4 pre-existing toasts now display the folded detail text instead of the opaque prefix — verified `loadAudit`'s 403 detection still works via `.status`. H-2: compose caller-supplied `AbortSignal` with the timeout signal via `AbortSignal.any` so a page-level cancel-on-navigate can't defeat the built-in fetch timeout; falls back to timeout-wins on older runtimes by spreading `opts` BEFORE `signal`. H-3: `openSocket(fn)` positional form now maps `fn → onEvent` (not `onNewAlert`); legacy pages calling the single-arg form were silently dropping ack echoes, node-status flips, and `auth_expired` notices — the last caused mystery `/login` redirects that were hard to trace. `new_alert` still reaches those pages via the DOM bridge, so nothing regresses. H-4 (monitor.jsx): hoist a single page-level 1 Hz `now` state into `MonitorPage` and pass it as a prop to every `NodeCard` so the header timestamp re-renders alongside the `SnapshotImage` underneath (whose own ticker was removed in the components.jsx cleanup below); without this the "last updated" clock froze for minutes on wall-mode displays.
    - **`8ac7d67 fix(spa)` — components.jsx snapshot 304 + mute-drawer truth-in-labeling (Selection/desync H-1..H-5):** components cluster. H-1: `SnapshotImage` cache-buster swapped from a 1 Hz local ticker to `?t=${node.snapshotTimestamp}` so the URL is stable across parent re-renders — the old ticker changed the URL every second even when the edge hadn't uploaded a new frame, defeating browser 304 and hammering the edge cams. Module-level shared-ticker infrastructure (`_snapshotTickListeners` / `_snapshotTickId` / `_startSnapshotTick` / `_stopSnapshotTickIfIdle`) removed — SnapshotImage was its sole consumer (grep-verified). H-2: `MuteDrawer` drops the local 30s `setNow` ticker — `n.snoozeMin` is a whole-minute integer refreshed only on the parent poll (~20s), so a sub-minute interval was re-rendering without changing the number, making "剩餘 X 分鐘" jump on every poll edge instead of ticking smoothly; smooth live countdown deferred to api.jsx exposing `snoozeUntil` (ms epoch). H-3: `disabled={muteState.global}` + tooltip on the 測試音效 buttons — `beep()` short-circuits on muted, so the old live-looking button emitting no sound read as "audio broken" to operators and generated support tickets; explicit "已靜音 — 請先取消靜音" tooltip surfaces the real cause. H-4: `unsnoozeAll` now uses `{...muteState, nodes: []}` instead of hardcoding `global`/`lightning` to false and `volume` to 70 — old code silently dropped an operator's intentional global mute during a planned drill just because they cleared stale per-node snoozes; button renamed "解除所有節點延期" (accurate scope) and disabled when there's nothing to unsnooze. **Latent bug found + fixed en route:** old code coerced `volume: null → 70`; new code preserves `null` (defensive against future refactors that stop always-defaulting the slider). H-5: `NodeSidePanel` cycles cell guards against div-by-zero — a pump with `cycles === 0` was rendering "每 Infinitym" instead of "—".
    - **Adjacent bugs surfaced but deferred (documented in audit doc back-fill):** `pumps.jsx` still renders `p.level` numerically on offline cards (tone is now stale but the number needs "—" substitution); `runRefreshAfterBulk` in `alerts.jsx` swallows errors; `monitor.jsx:59` prints `水位 null%` when pumps have no telemetry; `MonitorPage` reads `window.NODES` directly instead of via the React `nodes` state prop (benign today, bypasses the load-bearing `setNodes` contract); `ShiftBanner` mislabels `s.warn` as `s.snoozed` in one badge; `VolumeSlider` is a UI-only placebo; `handover.jsx writeGeneratedSummary` prints `undefined` when a section is missing; weather rain-unit inconsistency (`mm` vs `mm/h`) between the top-strip and forecast bars. All 8 items tracked in the follow-up commit slice section of `dashboard_audit_2026_07_16_ui_interaction.md`.
    - **Working pattern refinement (extends entry-28's "kill GLM, take inline"):** user's directive of "all claude" for entry-29 dispatch reflected an explicit tradeoff on top of the entry-28 GLM stall — the entry-27/28 hybrid pattern (Claude + GLM in parallel) is genuinely faster when both models are producing clean work, but the review-load cost of GLM-authored diffs (occasional over-refactor beyond the brief scope, e.g. Agent 5's ticker infrastructure removal was flagged and verified safe post-hoc) is real. For a HIGH-severity slice where every commit will land on `main` for immediate operator use, single-model dispatch trades a small wall-clock hit for lower integration risk. **Lesson: model-choice is a per-slice decision on the (review-load × severity × available reviewer bandwidth) tradeoff, not a global preference.**
    - **Cross-tier scope discovery:** Agent 3's session/auth work exposed a backend gap (`/login` didn't honor `?next=`) that neither the audit doc nor the frontend agent's brief had anticipated. Rather than dispatch a new agent, the reviewer completed the backend half inline — this is the "small file-scoped work is often cheaper inline than to re-brief" principle from entry 28 applied at a **cross-tier** granularity. Both halves shipped in a single commit (`163e399`) so the security invariant (no open redirect through `/login`) has an atomic history entry. Regression test written by the reviewer, not the agent, ensuring the frontend + backend contract is tested end-to-end at the HTTP layer.

30. **ChatGPT cross-verified fix sweep (2026-07-16, 7 commits `f3ce1a8`→`a72e384`; audit doc `dashboard_audit_2026_07_16_executive_summary.md` co-shipped) — external second-opinion audit produced 4 P0 release blockers + 7 P1s + 5 P2s against the current-state dashboard, all 4 P0s and 4 P1s worked as a 6-agent parallel Claude sweep after Claude verified each claim mapped to real code:** the user ran a fresh audit through ChatGPT after entries 28–29 had already landed. Rather than trust or dismiss the report on face, Claude first cross-referenced each of the 4 P0s and every P1 evidence line against the actual codebase (mapAlert STATE_MAP, alerts.py 400 requiring PENDING, api.jsx:627 HEAD preflight, audit.py:58 GET-only, refreshLive fan-out, WS ping branch, focus-hide class usages, pumps.jsx null-level branch) — all P0s and all spot-checked P1s confirmed as real. Only then dispatched 6 file-disjoint Claude agents; all six returned clean; one flagged a missing api.jsx wrapper (startStream/stopStream) that shipped as an inline follow-up in the same slice.
    - **`f3ce1a8 docs(operations)` — retract HLS/frame-step/download promise:** `docs/operations/dashboard-guide.md` promised operators HLS video preview with speed switching, frame-by-frame, and browser download in the ack-workflow section; the reality is that `pages/alerts.jsx:504` is an "honest placeholder" with a TODO, mapAlert exposes no playable URL, and the backend has PUT for MP4 upload but no GET route for playback. Rather than build the entire HLS feature (weeks), retract the promise to match reality: "MP4 稍後上傳 · HLS 預覽尚未接線", ops-SSH-to-central to view today, HLS/speed/frame-step/download listed as planned functionality pointing at the exec-summary P0 #3. "規劃中" not "即將推出" — no timeline promised.
    - **`8c7e9f8 fix(spa)` — components.jsx audio contract truthfulness (P0 #2):** StatusStrip's two audio-playback effects lied about their gating: (a) per-node snooze was ignored — a snoozed node still triggered a critical tone on new alerts; (b) severity was ignored — every unack-count increase called `playCritical()` regardless of the actual severity (the source comment even admitted this: "loudest tone is safer than silence"); (c) `muteState.lightning` was a UI placebo — flipping it did nothing. Fix: derive `newest` from `window.ALERTS` at effect-time, dispatch `playCritical`/`playWarning`/silent based on `newest.sev`, gate on both `muteState.nodes.includes(newest.node)` and `muteState.lightning`. Applied to both the count-increase branch AND the audioReplayIn branch. `muteState` added to useEffect deps so toggling snooze/lightning takes effect on the next tick, not the next unack. The docs-promised "點擊確認 → 播放確認音效" is closed in a separate app.jsx commit. TODO left in code: WS lightning-strike → `muteState.lightning` auto-flip still unwired (needs app.jsx/api.jsx coordination — separate slice).
    - **`a1ce003 fix(spa)` — pumps.jsx null-level display safety (P1 #5, pumps page):** an ONLINE pump with `water_level=null` was rendering as green/normal with a "null%" center readout — a stopped sensor on a live node presenting as healthy. Add `isNoTelemetry = !isOffline && (p.level == null)` branch between offline and critical. Tone amber (`sev-warn`), label "無水位資料", em-dash instead of "%", water-level fill container gets a dashed border so absent data reads distinctly from a zero-height "dry sump" fill. `p.level === 0` (real dry-sump) stays as normal-ok. Offline-first ordering from commit `0912d4f` preserved.
    - **`3e4c70c fix(spa)` — status.jsx per-row stream controls + null pump tone (P1 #4 + P1 #5 status-page half):** backend had `POST /api/stream/{id}/start` and `/stop` endpoints but the Status action column only had SnoozeRowButton — no way for an operator to start a stream from the dashboard. Add `StreamRowButton` mirroring SnoozeRowButton's per-row-busy + stopPropagation pattern; camera-only render; toggle inferred from `bitrate > 0`; Play/Pause icons. Also hardens the pump-row water-level cell to render "—" in sev-warn when `n.level == null`, mirror of the pumps.jsx fix above so the Status page's pump display doesn't lie either. Stream buttons stayed disabled with "串流控制 (等待 API)" tooltip until the follow-up commit landed the api.jsx wrappers.
    - **`b7e71d8 fix(spa)` — api.jsx CSV preflight + refreshLive + WS onPing (P0 #4 + P1 #1 + P1 #2):** three api.jsx fixes. (P0 #4) `exportAuditCsv` HEAD preflight was returning 405 because FastAPI's `APIRouter.get` doesn't auto-implement HEAD — the download button always threw and no CSV was ever produced from the visible export path. Replace the HEAD probe with a GET against the sibling `/api/audit?limit=1` (same admin-only gate as `/api/audit/export.csv`) via apiFetch so 401/403 surface with the same detail flow; only after the auth-check succeeds does the anchor-click download fire. (P1 #1) `refreshLive` only re-fetched nodes/alerts/history/rate — peer handover edits, post-503 weather recovery, and audit rows stayed stale until a full page reload. Add `loadWeather` / `loadHandover` / `loadAudit` to the `Promise.allSettled` fan-out; all three were already module-scoped from `loadInitial`. Return-shape extended in both success + catch paths. (P1 #2) `ws.onmessage` ping branch was `return;` with no signal to the outer app, so `liveSec` drifted to 20s per poll cycle and StatusStrip painted "Reconnecting…" for ~half of every cycle even when the WS was healthy. Add `onPing` to the `openSocket` options; ping branch calls it before returning so callers can reset a staleness counter without leaking 'ping' into the general event whitelist.
    - **`9f3230b fix(spa)` — PENDING_VIDEO ack UX + ack sound + filtered empty (P0 #1 + P0 #2 sound + P1 #6):** three alerts+app fixes. (P0 #1) `api.jsx` STATE_MAP collapses backend `PENDING_VIDEO` → `'pending'`, so AlertRow/AlertDetail rendered the 認領 button enabled — but the backend rejects an ack with 409 until the MP4 uploads and status flips to `PENDING`. Since api.jsx was reserved for Agent 3, use a client-only heuristic: `isWaitingForVideo = alert.state === 'pending' && !(alert.timeline || []).some(t => t.label === 'UPLOADED')` — mapAlert only pushes UPLOADED when `mp4_path` is truthy, so absence is a reliable proxy. Disable Ack button, relabel "等待影像上傳中", show "等待影像" pill in row + header; keyboard `A`/`Shift+A` gated identically with a warn toast so keypress isn't silently ignored. Bulk paths read `resp.acked` / `resp.resolved` (backend shape confirmed against `api/alerts.py:667`/:720) and preserve selection + note on total-failure with an explicit "都不可認領" message; partial-failure shows "N / M 成功" then clears. (P0 #2 sound) `onAck` now calls `SDPRS_AUDIO.playAck()` after the ack success path, closing the docs-guide promise "點擊確認 (Ack) → 播放「確認」音效"; gated on `!muteState.global` (StatusStrip mute toggle doesn't mirror to `SDPRS_AUDIO.setMuted` — only MuteDrawer does), wrapped in try/catch so a WebAudio failure never eats the ack. (P1 #6) `FilteredEmptyState` distinguishes "underlying list has alerts but current filter hides all" (amber Filter icon + hidden count + 清除篩選 button that resets severity + search + tab) from the true "system OK" (green). Old code showed the green all-clear for both cases.
    - **`a72e384 fix(spa)` — api.jsx startStream/stopStream wrappers + app.jsx wire onPing:** two loose ends from the parallel sweep. Agent 4 had gated the Status page stream buttons on `typeof api.startStream === 'function'` because the wrapper didn't exist; adds `startStream(nodeId)` → `POST /api/stream/{id}/start` and `stopStream(nodeId)` → `POST /api/stream/{id}/stop` via apiFetch (so 404/503 detail messages surface to the operator's toast), and adds both to the `window.SDPRS_API` export. Agent 3 added `onPing` to the `openSocket` options surface but couldn't wire the callback (app.jsx was off-limits for that agent); adds `onPing: () => setLiveSec(0)` in app.jsx's `openSocket` call. Status buttons and the liveSec reset are now live without any further agent work. P1 #2 + P1 #4 fully closed end-to-end.
    - **`docs/superpowers/dashboard_audit_2026_07_16_executive_summary.md` (co-shipped):** the ChatGPT-produced audit itself, referenced by all 7 fix commits. 16 residual clusters — 4 P0 (all worked here except HLS which shipped as a docs retraction), 7 P1 (4 worked here: audio contract audio subset, HEAD, refreshLive fan-out, stream controls, null-pump, filtered empty; 3 deferred: focus mode, mobile-safe alerts, healthy-WS-shows-Reconnecting *already-closed-here*), 5 P2 (all deferred). Notable: report accurately identified that `SDPRS_API` didn't have stream wrappers, that api.jsx:139 `PENDING_VIDEO` collapse mismatched api/alerts.py:400 backend contract, that audit.py:58 lacked a HEAD method, and that ws.onmessage:714 didn't reset liveSec — all four were exact-line-cite accurate. Also correctly flagged 4 pytest "setup errors" that are collection-order-dependent (some test modules overwrite `SECRET_KEY` with a 27-char test value; node-allowlist lifespan later rejects it under the min-secret validation from `config.py:293`) — pre-existing, not caused by this slice, flagged for a separate cleanup.
    - **Working pattern captured (extends entry 28's "verify before fix"):** an external second-opinion audit is worth taking seriously *only after* cross-referencing every concrete claim against the actual code. ChatGPT's report cited exact file:line numbers for every finding — Claude Read/Grep-verified each against the current source before dispatching agents. Zero hallucinated findings; every P0 and every P1 spot-checked mapped to real code. Cost was ~20 minutes of verification for ~90 minutes of parallel agent work, and the resulting fix commits reference the audit doc so the audit trail is atomic. **Lesson: an audit's cite quality is the trust signal — cite-exact reports earn parallel-agent dispatch; cite-fuzzy reports get single-agent verify-first.**
    - **Cross-model collaboration note:** this slice mixed ChatGPT (external audit producer) and Claude (verifier + fixer + reviewer). Previous slices mixed Claude and GLM as co-fixers. Both work. The pattern is more general than a specific model: use one model to produce a fresh perspective on the current-state code, then have another model verify + implement. Prevents the "I wrote the code so I don't see the bug" blind spot without requiring a second human reviewer.

31. **Dashboard MEDIUM/LOW cleanup slice (2026-07-19, 9 commits `6e3a181`→`29cff78`) — closes the residual 21 MEDIUM/LOW findings from `dashboard_audit_2026_07_16_ui_interaction.md`; hybrid dispatch (4 files completed inline after prior session hit the daily cap, then 3 file-disjoint parallel Claude agents finished the remaining 4 files):** entry 30 landed the ChatGPT-verified P0/P1 slice; entry 31 completes the MEDIUM/LOW residual. The prior session had dispatched 7 parallel agents against the MEDIUM/LOW list and 5 of 7 hit the Anthropic session cap ("resets 12am Asia/Taipei") — only Agent B (app.jsx) completed cleanly, leaving partial edits from Agent F (api.jsx trailing-debounce flag added but never acted on) and additional partial work on alerts.jsx (page-toast state added, handleBulkResolve not migrated) discovered when the session resumed. Recovery: verify each dirty file's diff, complete the partial work inline, ship as file-scoped commits, THEN dispatch 3 remaining agents against a clean tree — exactly the entry-27 Auth-5 "verify disk state on API-error mid-run before re-dispatching" pattern applied at a session-cap boundary. All banned untracked paths (`docs/sdpr_UI_V2.zip`, `docs/sdpr_UI_V2/`, `docs/ui_redesign_v2_prompt.md`) remained untracked throughout — verified via `git status --short` before each commit.
    - **`6e3a181 fix(spa)` — app.jsx MEDIUM cleanup (B1-B5):** five keyboard/reconnect/persistence fixes from the prior session's only surviving agent, verified diff-clean and committed as-is. B1 `Alt+←` `preventDefault`s only when `pageHistoryRef.current.length > 0` — first-load users can still leave the page via browser back instead of being silently trapped. B2 Cmd/Ctrl+K idempotent (`setCmdkOpen(v => !v)`) — a second press closes the palette instead of no-op'ing. B3 session-expiry modal focus trap: `sessionModalButtonRef` on the sole focusable button; a `useEffectA` on `sessionExpired` installs a keydown listener that `preventDefault`s Tab/Shift+Tab and bounces focus back to the button so keyboard focus can't escape to controls behind the backdrop; `document.activeElement` saved on entry and restored on cleanup. B4 focusMode persisted to `localStorage['sdprs.focusMode']` via lazy `useStateA` initializer + a write-back effect — Safari private-mode guarded by try/catch. B5 `lastPingRef` initialised to `Date.now()`; a >30s gap between `onPing` calls (pings are ~10s on a healthy WS) unambiguously means the socket dropped and reconnected, so we `scheduleRefresh()` + `showToast('連線恢復 — 已重新同步資料', 'info')` — operators are told they may have missed events instead of silently drifting.
    - **`7d04ee3 fix(spa)` — monitor.jsx null-guards + tab sessionStorage persist + drop [F] label (MED C):** three related monitor-page fixes. Removed the `[F]` shortcut label from the fullscreen button — no global `F` handler exists (audit doc line 116); the label was cargo-culted from an unwired shortcut. Persist the tab filter (`'all'|'camera'|'pump'`) to `sessionStorage['sdprs.monitor.tab']` via lazy `useState_p` initializer + write-back effect — sessionStorage (not localStorage) so a fresh browser session resets to 'all' but a navigate-away-and-back preserves the operator's chosen slice. Guarded `NodeCard.heartbeat` / `.upload` / `.cycles` and `PumpCard` water-fill height + centered readout against `null` — renders `—` (or 0% fill) instead of `null%` / `null×` on partial-row nodes; also the pump-tank water fill uses `(node.level ?? 0) + '%'` so absent telemetry doesn't fabricate an inline-CSS `NaN%` string that browsers would ignore silently.
    - **`adde28e fix(spa)` — alerts.jsx bulk-action UX (MED A1-A2):** replaced four blocking `window.alert()` calls in `handleBulkAck`/`handleBulkResolve` with a page-level `pageToast` (state + `useEffect_p` 4s auto-dismiss + `role="status" aria-live="polite"` fixed-position render surface at the bottom of the page) so a NOC operator's keyboard focus is never dropped mid-triage; toast tone is `error` / `warn` per outcome. Refactored `runRefreshAfterBulk` to track `attempted` + `lastErr` and rethrow when every attempted refresh path failed — the bulk handlers now defer `setChecked(new Set()) + setBulkNote('')` until AFTER `runRefreshAfterBulk` succeeds so a mid-flight network hiccup preserves the selection + typed note for retry ("批次已提交但刷新失敗，請手動刷新" toast). `handleBulkResolve` mirrors the same pattern (was window.alert-only in the prior session's partial edit).
    - **`d47156a fix(spa)` — api.jsx refreshLive trailing debounce + WS reconnect cancellation (MED F1-F2):** the prior session's partial Agent F edit had added a `refreshLivePending` flag but never wired the finally-block scheduler. Completed inline: `.finally` now resets the pending flag FIRST (so a new burst during the follow-up can flip it again) then `setTimeout(() => refreshLive().catch(() => {}), 300)`. Bounded: a burst of 10 `node_status` events during one in-flight refresh now collapses to exactly ONE follow-up refetch instead of 10 back-to-back fan-outs. Also added defense-in-depth `reconnectTimer` tracking to `openSocket`: the connect scheduler stores its setTimeout id, the teardown thunk clears it. Previously, a reconnect scheduled from `ws.onclose` between the auth_expired message arriving and app.jsx invoking `wsStopRef.current()` would still fire — `connect()` did early-return on `closed` so nothing catastrophic happened, but the timer leaked. Now the teardown cancels the pending timer as well as clearing the stable timer.
    - **`2052769 fix(spa)` — components.jsx a11y (MED E1-E3; parallel Agent):** three components fixes from the parallel dispatch after the session cap reset. E1 `MuteDrawer` + `NodeSidePanel` capture `document.activeElement` on open into a `lastFocusedRef`, restore on effect cleanup via try/catch (trigger may have unmounted mid-refresh) — WCAG 2.4.3 focus restore. E2 `CommandPalette` full ARIA combobox/listbox pattern: input gets `role="combobox"` + `aria-controls="cmdk-listbox"` + `aria-autocomplete="list"` + `aria-activedescendant={\`cmd-item-${hi}\`}` when results exist; results container gets `id="cmdk-listbox"` + `role="listbox"` + Chinese `aria-label`; each item gets stable `id={\`cmd-item-${i}\`}` + `role="option"` + `aria-selected` — arrow-key highlight is now announced to screen readers instead of silent. E3 `FilterChip` gains optional `onClear` prop that renders a `×` affordance INSIDE the chip using a `<span>` (avoids nested `<button>` HTML violation by combining `stopPropagation` on the outer chip's click) plus a `Delete`/`Backspace` keyboard handler on the chip's onKeyDown when focused — backwards compatible, callers without `onClear` render byte-identically to before. Agent confirmed the `cycles === 0 → Infinity` guard was already in place at `components.jsx:1362` from the prior HIGH slice; no change.
    - **`7b2233a fix(spa)` — weather.jsx D-slice (MED D1-D3; parallel Agent):** three weather-page fixes. D1 removed the "雷擊期間自動靜音" checkbox and its `useState_p` state — grep confirmed `SDPRS_MUTE.setLightningAuto()` doesn't exist and no weather-WS → snooze fan-out ever shipped; placebo UI worse than absence. Anchor comment marks the restoration path if the pipeline ships. D2 split loading vs unavailable: `typeof window.WEATHER === 'undefined'` now renders `<EmptyState title="載入中…" hint="正在取得天氣資料"/>`; the pre-existing `!w.available` branch retitled to "天氣資料暫時不可用". api.jsx never assigns `window.WEATHER` until fetch settles, so `undefined` = pre-fetch and `{available:false}` = fetch completed but backend refused. D3 unified rain unit: hero "mm (10min)" was a lie — backend Open-Meteo `precipitation` is per-hour and SMG `rainfall_24h_mm` reads from `rainfall_hourly` per `services/weather_service.py:157`; no 10-min bucket exists. Changed to "mm/h" matching the forecast legend below. D4 + LOW-handover-{beforeunload,TTL} were already fixed by entry 29's `390622d` (handover draft rescue + TTL badge); agent confirmed and skipped.
    - **`8ebf623 fix(spa)` — status.jsx MED G (G1 + G2; parallel Agent):** two status-page fixes. G1 `SnoozeRowButton.trigger` no longer silently `return`s when `SDPRS_API.snoozeNode` is missing — routes through the existing `onError` prop so the parent's toast handler surfaces "暫時無法連線後端，請稍後再試" instead of the operator wondering why their click did nothing (defensive UX; the real bug is server-down, this just tells the operator instead of failing silently). G2 three `FilterChip` clear `×` buttons (type / status / location) were bare `<button>`s nested inside `FilterChip`'s own `<button>` — invalid HTML (browsers unwrap or split the tree). Replaced each with `<span role="button" tabIndex={0}>` carrying the original click's `stopPropagation` + clear plus a keyboard `onKeyDown` (Enter/Space) handler so the affordance stays keyboard-operable without breaking the DOM. Visual identical.
    - **`e37524b fix(spa)` — audit.jsx LOW G4 (parallel Agent):** CSV `limit` in `exportAuditCsv` opts bumped from `1000` to `10000` — docs advertise 10000 as the ceiling; the prior 1000 was a 90% silent under-fetch that made "matched but truncated" invisible to compliance. Added "最多 10000 筆" caption next to the export button + updated `title=""` so operators know when they're at risk of a silent truncation and should narrow filters instead.
    - **`e3f03a9 security(spa)` — tweaks-panel.jsx MED G5 outbound postMessage tighten (parallel Agent):** the inbound message listener's same-origin gate already shipped as part of entry 28's `4f058dc`, but three outbound `window.parent.postMessage(..., '*')` calls remained — `__edit_mode_set_keys` (leaks tweak edit payload), `__edit_mode_available` (fingerprints SDPRS presence), `__edit_mode_dismissed`. Tightened all three to `window.location.origin` to mirror the inbound gate's symmetry — nothing legitimate reads these from a cross-origin parent. `window.postMessage({...}, '*')` at line ~237 (`__deck_rail_visible`) is a same-window dispatch and correctly left as `'*'` (cannot escape the current document).
    - **`29cff78 fix(spa)` — app.jsx LOW #5 mobile-nav focus restore:** picked up inline (not in any parallel agent's scope). New `mobileNavButtonRef` on the hamburger + `mobileNavWasOpenRef` state tracker; the existing `mobile-nav-open` classlist-toggle effect now also refocuses the hamburger on true → false transitions with `focus({preventScroll:true})` to avoid a jarring auto-scroll to the pinned hamburger position. `wasOpen` ref prevents firing on initial mount where `mobileNavOpen` is already false and no drawer was ever open. Same pattern as MuteDrawer / NodeSidePanel from E1 but without capture-on-open — the hamburger is the sole, stable trigger.
    - **Deferred adjacent items (documented for a possible follow-up slice):** `pumps.jsx:43` sensor-conflict banner has no operator dismiss/ack — deferred as a product decision (a "critical" hardware-fault banner arguably SHOULD persist until the underlying condition clears, and operators should not be able to acknowledge-away a still-active fault; the correct fix is auto-clear when `sensorConflict` flips false, not adding a dismiss button). Four pytest `collection-order` errors under min-secret validation (entry 30 flagged) — pre-existing, needs a per-test-module secret-override cleanup, not this slice.
    - **Working pattern captured (extends entry 27's "verify disk state on API-error"):** the session-cap boundary is a special case of the mid-run failure pattern. When Anthropic's daily cap fires mid-dispatch, some agents complete cleanly, some produce partial edits (state added but consumer wiring not, half of a two-branch fix landing but not the other), and some don't start at all. Recovery protocol: (1) `git status --short` + `git diff --stat` to see what's dirty; (2) per-file diff review to categorize each as clean-complete / partial / stale-noop; (3) complete partials INLINE (Claude-native round-trip is usually cheaper than re-briefing an agent for small file-scoped work — same principle as entry 28's "kill GLM, take inline"); (4) commit each file as a file-scoped commit BEFORE dispatching the residual agents, so the residual dispatch starts from a clean tree with no accidental cross-contamination from the partial state. This slice: 4 files inline (app.jsx already clean-committed, monitor.jsx already clean-committed, alerts.jsx partial completed + committed, api.jsx partial completed + committed), THEN 3 parallel agents against clean tree, THEN inline pickup of the one LOW item nobody was assigned. Zero re-briefing cost, all 9 commits atomic.
    - **Slice completes the residual from `dashboard_audit_2026_07_16_ui_interaction.md`.** Entries 28 (BLOCKER/CRITICAL/top HIGH) + 29 (remaining HIGH) + 30 (ChatGPT P0/P1) + 31 (MEDIUM/LOW) sequence closes the entire 64-item audit end-to-end across 30+ file-scoped commits. Remaining audit-listed items are either explicitly product/decision-required (deferred list above) or already-shipped in an earlier slice (D4, LOW-handover-beforeunload, LOW-handover-TTL, MED alerts:305 SnoozeMenu key bubble, MED alerts:99 selected-alert-out-of-filter, MED pumps:19 null-level, MED api.jsx:597 exportAuditCsv HEAD — all fixed by entries 28-30 and skipped by parallel agents after verify).

---

## 2. Executive summary (current)

| Dimension | State |
|---|---|
| **Production MVP** | Deployed on LAN (Pi 5 central + Pi 5 glass edge), SQLite/WAL. See root `MEMORY.md`. |
| **`main`** | @ `c0ff3b1`, **pushed to `origin/main`**. Carries pump reconstruction + V2 SPA + all 2026-07-13 theme slices + the 2026-07-14 PG-safe server slice + the audience-based docs tree + the full-audit fix slice. |
| **Pump-merge SDD effort** | ✅ **Complete, reviewed, merged, pushed.** ⚠️ **Not bench-commissioned on hardware** (spec §6). |
| **V2 SPA dashboard** | ✅ On `main` (~4,900 LOC `.jsx`). Two security items fixed; detector-health pills added; perf (blanket-refresh) still open (Theme 5). |
| **Theme-6 glass slice** | ✅ Silent-blinding, dead-simulate, stale-pairing, detector-health telemetry (`22e6084`, pushed) + **health now surfaced end-to-end in server/dashboard** (`3c110a2`, local). Remainder open (single-sensor fallback, blocking encode, buffer arithmetic). |
| **Theme-4 data-lifecycle** | ✅ Retention delimiter (~24h boundary), `pump_readings` pruning, orphan-MP4 sweep, `weather_config` PG-startup wipe all fixed (`dddda76`, pushed). Remainder: retention PG-portability, unverified backups. |
| **Theme-2 auth-hardening** | ✅ UTF-8-safe constant-time compares, per-IP login throttle, session-cookie flags, edge node_id allowlist, MQTT auth+ACL deploy template (`c4660d8`, pushed). ✅ Snapshot GET now auth'd (`e73718d`). Remainder: per-node API keys, storage-endpoint auth + path-traversal review, WS-session tightening. |
| **Theme-5 observability** | ✅ **Complete.** Request-storm coalescing, WS HOL-blocking fix, offline-mark TOCTOU fix (`9d3f063`), `/cycles` N+1 → batch (`525452e`), glass MQTT Last-Will → instant offline (`461c0ed`), **instant-ONLINE recovery push + dead `broadcast_node_status` deleted** (`e73718d`) — node up/down both push live. |
| **Theme-3 glass autonomy** | ✅ Upload gives up on 4xx / after max retries, MP4-missing → FAILED, non-blocking MQTT connect at boot (`73b5af0`). ✅ **blocking MP4 encode + buffer arithmetic → non-blocking capture implemented** (`34efdf6`, dormant behind `capture.async_encode`, awaiting §8 hardware gate). No open code items — pump sensors still need the hardware bench (spec §6). |
| **Tests** | **315 passing** (55 pump + 131 server + 129 glass). Zero failures. |
| **Biggest remaining risk** | **Hardware commissioning gate** for pump sensors (spec §6). Cloud cutover still wants a live-PG smoke test + retention PG-portability (T4), but the T1 request-path 500s are closed (`e73718d`). Neither affects the deployed SQLite LAN MVP. |

**One-line status:** Pump-merge is shipped and green on `main`; the next work is the open reconstruction themes and the pump hardware bench pass — not fixing what shipped.

---

## 3. Verified test status (run 2026-07-16, on `main` @ `1291eb0`)

| Suite | Command | Result |
|---|---|---|
| edge_pump | `cd edge_pump && python -m pytest tests -q` | **55 passed** |
| central_server | `cd central_server && python -m pytest tests -q` | **131 passed** (36 warnings) |
| edge_glass | `cd edge_glass && python -m pytest tests -q` | **129 passed** |
| **Total** | | **315 passed, 0 failed** |

Environment: Python **3.14.3**, pytest **9.0.2**, FastAPI **0.135.1**. (edge_glass +15 from non-blocking capture, +2 from glass-LWT, +11 from T3, +33 from T6, **+5 audit-fix slice** (encoder contract ×2, audio fixed-mode/forensics ×2, uploader purge ×1 net). central_server +4 glass-LWT, +4 batch-cycles, +9 T5, +18 T2 auth, +7 T4, +23 from the 2026-07-14 server slice (dual-backend dispatch · snapshot auth · online-recovery broadcast), **+16 audit-fix slice** (get_node regressions, ack-field mapping, handover/audit PG dispatch, same-day window, cmd topics). edge_pump **+7 audit-fix slice** (drift guard, polarity, timeouts, battery ship-off). The SPA `app.jsx`/`api.jsx` changes + the `edge_glass_main.py` async wiring have no automated test — verified by diff review + a runtime integration smoke test.)

> **pytest + `[Cloud]` path trap:** a bare `pytest` from `edge_glass/` fails with *"path cannot contain [] parametrization"* — pytest parses the `[Cloud]` in the rootdir as a test-id. Pass an explicit **relative** test path instead: `python -m pytest tests -q`.

**Caveats worth fixing (unchanged):**
1. **edge_glass import convention diverges** — bare imports (`from utils…`, `from detectors…`) only collect when `edge_glass/` is the CWD; `pytest` from repo root fails collection. CI/portability trap; normalize so one command runs all three suites.
2. ~~**`datetime.utcnow()` deprecation** — 29 call sites in `central_server`~~ ✅ **DONE** (`3a61fef`) — all 32 production sites → `central_server/timeutil.utcnow()` (naive-UTC helper). Test-file `utcnow` intentionally left (cosmetic).

---

## 4. Module inventory (verified line counts)

| Module | .py files | LOC (src+tests) | Test result | Notes |
|---|---|---|---|---|
| `edge_pump` | ~19 | ~1,500 | 48 ✅ | Reconstructed: pure `control_logic.decide()`, `sensors` HAL, thin guarded `main`. `water_sensor.py` removed. Desktop-testable. |
| `central_server` | ~28 | ~7,600 | 44 ✅ | FastAPI + SQLite/WAL (PG path present but see Theme 1). 8 API routers, 6 services. |
| `edge_glass` | 25 | ~5,550 | 63 ✅ | Detection (audio/visual/trigger), buffer, comms, MP4, snapshot, thermal, RTSP. Not touched by pump-merge. |
| SPA (`static/spa`) | 8 `.jsx` + vendor | ~4,900 | no automated test | React/Babel/Tailwind in-browser. **No headless JSX test harness** — a real gap; SPA-side fixes are verified by reasoning + server-side tests only. |

---

## 5. Pump-Merge SDD effort — ✅ COMPLETE, MERGED, PUSHED (un-commissioned)

Spec: `docs/superpowers/specs/2026-07-10-pump-merge-and-reconstruction-design.md`
Plan: `docs/superpowers/plans/2026-07-10-pump-merge.md`
Detail ledger: `.superpowers/sdd/progress.md` (11 tasks + final-review record + merge outcome)

All 11 tasks landed review-clean; the final whole-branch review + fixes are done; merged and pushed. Delivered:
- Pure, hardware-free `control_logic.decide()` — full safety ladder (guarded conflict override w/ bounded bursts + 15-min ceiling, dry-run interlock, max-runtime duty cycle, rain threshold-lowering, dry-off delay) with exhaustive desktop tests.
- New `sensors.py` HAL (debounced digital + analog median, per-sensor `valid`/`None` degradation, injectable reader).
- Thin **guarded** `main.py`: WDT-on-by-default (fed only after a successful iteration), guarded init, fixed missing `machine` import, `ticks_ms` throttle, LWT before connect, bounded socket I/O.
- Extended MQTT payload end-to-end: server parsing, 2 new `pump_readings` columns (SQLite + PG), WebSocket push, SPA pump card (rain / dry-run / **sensor-conflict CRITICAL**).
- **Theme-5 first slice:** asyncio loop captured in the FastAPI lifespan → pump broadcasts from the paho thread no longer swallowed.
- **Deploy path corrected** (final review): provisioning now copies the new modules; node boots.

**Remaining gates before FIELD-ENABLING the new sensors:**
- ⚠️ **Hardware bench commissioning (spec §6)** — student sketch, wiring doc, and toolkit pinout disagree on pin/polarity. Bench-verify per-sensor polarity + raw ADC at known dry/full states. Manual rollout step, **not done**.
- ⚠️ **No automated frontend test** for the pump-card indicators.
- ✅ Sensors now genuinely ship OFF (`FLOAT_ENABLED=False`/`RAIN_ENABLED=False`, reconciled by the 2026-07-13 audit `71ec41a`), reproducing analog-only behavior until §6 bench commissioning flips them True for field use.

---

## 6. Six-theme reconstruction tracker

The pump-merge spec's Appendix decomposed a full audit into six themes. Pump-merge shipped the pump slice + one observability slice; the Theme-2 security slice (2 items) shipped 2026-07-13. Current status (✓ = re-verified by code read):

| # | Theme | Status | Detail |
|---|---|---|---|
| 1 | **Unify data-access layer** | 🟢 **Request paths closed** (`e73718d`) | Was: `get_db()` raises under PG → dashboard/alert-response call sites **500'd in PG mode**. Now: `get_backend()` accessor + dual-backend `list_events`/`get_event_counts`/`acknowledge_event`/`resolve_event` + 3 dashboard helpers; all 6 bypass sites in `main.py`/`api/alerts.py` unified — **no production request path calls `get_db()` under PG**. ✅ **`c0ff3b1` closes the stragglers:** handover GET/PUT + dashboard context (dual-backend `set_handover_note`/`get_effective_handover_note`) and `audit_service` (`_pg_log_action_sync`/`_pg_list_actions_sync` — audit trail was silently swallowed under PG). Remaining before a real cloud cutover: retention `datetime()` PG-portability (ties to T4) + a live-PG smoke test (dispatch is unit-tested; never run against real PostgreSQL). |
| 2 | **Close the trust boundary** | 🟡 **Advancing** (`c4660d8`) | ✅ `SECRET_KEY`/`EDGE_API_KEY`/dashboard creds `required=True` (fail-closed). ✅ SPA inline-script injection fixed (`55b351a`), spoofable `resolved_by` fixed (`4a8bdc2`). ✅ **NEW 2026-07-13 (`c4660d8`):** UTF-8-safe **constant-time** compares (`_ct_equal`) for API key + dashboard creds; **login throttle** (per-IP, 429 lockout); cookie **`SameSite=Lax` + `Secure` (COOKIE_SECURE)**; **`node_id` allowlist** on alert/snapshot ingest (`ALLOWED_NODE_IDS`, empty=allow-all); **MQTT auth+ACL** deploy template (`mosquitto_acl.conf` + `MQTT_SECURITY.md`, secure-mode block in `mosquitto.conf`). ✅ **Snapshot GET auth'd (`e73718d`)** — `verify_api_key_or_session`; same-origin `<img>` carries the session cookie, dashboard unaffected. 🔴 Still open: placeholder detection warn-only, shared static `EDGE_API_KEY` (no **per-node** creds), storage-endpoint auth + storage-path traversal review, WS-session tightening. |
| 3 | **Harden edge offline-autonomy** | 🟡 **Pump done, glass advancing** (`73b5af0`) | ✅ Pump slice delivered (WDT, bounded I/O, guarded init, LWT). ✅ **NEW 2026-07-13 (`73b5af0`):** glass upload gives up on 4xx / after `MAX_RETRIES` (was infinite 1s retry — now interacts safely with the T2 403 allowlist); MP4-missing → terminal `FAILED` instead of a false `UPLOADED` (silent clip loss); glass MQTT `start()` uses non-blocking `connect_async()` + always `loop_start()` so a broker down at boot self-recovers (was: early-return defeated paho auto-reconnect). 🔴 Remaining: **blocking MP4 encode on the capture loop + buffer arithmetic** (deferred — post-frames-from-buffer redesign needs camera-thread-safety + hardware validation); ⚠️ bounding `umqtt connect()` on the *pump* still needs a socket-factory/library change + hardware. |
| 4 | **Data-lifecycle correctness** | 🟡 **Advancing** (`dddda76`) | ✅ **Fixed 2026-07-13:** retention delimiter ~24h boundary error (`datetime()`-normalized both sides in retention_service + event_service helpers); `pump_readings` now pruned (was unbounded); orphaned-MP4 fail-safe sweep added; `weather_config` no longer wiped on every PG startup. 🔴 Remaining: retention is SQLite-only (`datetime()` — PG portability, ties to T1); "trusted client timestamps" = non-issue for event retention (uses server `created_at`) but `pump_readings` prunes by edge-supplied `timestamp` (edge-clock trust, accepted); unverified backups. |
| 5 | **Make observability real** | 🟡 **Advancing** (`461c0ed`) | ✅ MQTT-thread WS loop-capture fixed (pump card live). ✅ glass heartbeat emits `visual_health`/`audio_health` (`22e6084`), **server/dashboard consume + surface them** (`3c110a2`). ✅ **(`9d3f063`):** SPA request-storm fixed (300ms debounce + in-flight guard); WS broadcast **head-of-line blocking** fixed (concurrent `gather` + 5s timeout); offline-mark **TOCTOU false alarm** fixed. ✅ **`/cycles` N+1 → one batch call** (`525452e`). ✅ **glass MQTT Last-Will → node OFFLINE the instant it drops ungracefully** (`461c0ed`), vs waiting out the 90s heartbeat timeout. ✅ **instant-ONLINE recovery push (`e73718d`)** — transition-only `node_status: ONLINE` from the MQTT thread (prev_status captured under lock); dead `broadcast_node_status` deleted. Node up/down both push live — **theme complete**. |
| 6 | **Detection correctness (glass)** | 🟡 **Advancing** (`c0ff3b1`) | ✅ **Fixed 2026-07-13:** silent permanent visual blinding (re-baseline + `blinded` flag); dead `--simulate`/`simulate_trigger` path; stale-pairing fusion; detector-health telemetry + audio ring-buffer vectorization + camera-reopen + audio staleness; **health surfaced end-to-end** (`3c110a2`). ✅ **blocking MP4 encode + buffer arithmetic → non-blocking capture** implemented dormant behind `capture.async_encode` (`34efdf6`; buffer invariant now asserted via `clamp_capture_window`) — awaiting §8 hardware gate. ✅ **Fixed 2026-07-14 (`c0ff3b1`):** async-encode `(ts, frame)` contract break (path would have dropped every event); fixed audio mode can now actually trigger (**-30 dBFS**, was SPL-scale 90); alert forensics `db_peak`/`freq_peak_hz` now real (were always 0.0); drills marked `is_simulation` + no longer consume the real-alert cooldown. 🔴 Remaining: AND-only fusion w/ no single-sensor fallback (product/safety decision); post-crack persistent-visual + storm-audio phantom hardening beyond cooldown. |

**Each open theme is its own spec → plan → implementation cycle.** Appendix severities assume the internet-exposed PostgreSQL/EMQX deployment; several drop a notch on the LAN + SQLite Pi deployed today.

---

## 7. Cross-cutting tech debt

1. ~~**`datetime.utcnow()` × 29** in `central_server`~~ ✅ **DONE** (`3a61fef`) — migrated to `timeutil.utcnow()`. (§3)
2. **edge_glass test portability** — bare imports break `pytest` from repo root; no single top-level command runs all suites. (§3)
3. **No headless JSX test harness** — SPA changes are verified by reasoning + server-side tests only. Consider a minimal DOM/JSDOM harness before further SPA work.
4. **Ledgers are load-bearing** — debt is tracked in docs (this file + `.superpowers/sdd/progress.md`) rather than inline TODOs; keep them current.

---

## 8. Deferred / needs explicit go-ahead

| Item | Note |
|---|---|
| **Pump hardware bench commissioning** | Spec §6. **Blocks field-enabling** the new sensors. Not started. Ship analog-only until done. |
| Weather UI | Backend shipped 2026-05-03 (gated by `CWA_API_KEY`); SPA already references weather (36 `.jsx` hits). Confirm remaining scope vs "done". |
| ESP32 battery firmware | Battery/power telemetry preserved in payload (`_read_power`); on-device sensing firmware pending. |
| MQTT downlink of snooze config to edge | Server-side snooze exists; downlink not built (spec excludes a cmd topic). |
| Untracked docs | `docs/sdpr_UI_V2.zip`, `docs/sdpr_UI_V2/`, `docs/ui_redesign_v2_prompt.md` — decide: commit or `.gitignore`. |

---

## 9. Recommended next actions (prioritized)

1. ~~Merge `spec/pump-merge → main`~~ ✅ **DONE 2026-07-13** (merged + pushed).
2. 📌 **TODO — "Hardware Day" (highest-leverage single action):** one scheduled on-site/bench session clears all three hardware gates at once. Checklist:
   - **(a) Site network fix — the only deployment blocker.** Wired Ethernet between the Pis, or router change to disable WiFi AP isolation. Verify: edge Pi (`172.16.60.201`) reaches server Pi (`172.16.60.187`) on `:8000` (REST) and `:1883` (MQTT); then end-to-end: heartbeat → dashboard shows ONLINE, simulated alert → JSON <1s → MP4 upload → clip playable.
   - **(b) §6 pump bench commissioning.** Bench-verify float-switch and rain-DO polarity at known states (matches `FLOAT_ACTIVE_LOW`/`RAIN_ACTIVE_LOW`); record raw ADC at known dry/full levels; only then flip `FLOAT_ENABLED`/`RAIN_ENABLED` → `True` in `edge_pump/config.py`, re-flash, and confirm the dashboard pump card's rain/dry-run/conflict indicators respond. **⚠️ Since `c0ff3b1` the `*_ACTIVE_LOW` flags also select the pull resistor** (active-low→PULL_UP, active-high→PULL_DOWN, pulling toward de-asserted) — a wrong flag now means wrong pull AND inverted reading, so verify each flag against the physical wiring FIRST, including the unwired-line reading (must read not-asserted, never pump-ON).
   - **(c) §8 async_encode gate** (bench Pi 5 + camera). Confirm: clip window `[T-pre, T+post]` correct on real hardware; capture loop never blocks during encode; a second event during post-roll is detected; snapshot stream doesn't freeze; memory soak stays within budget — note the steady-state buffer is ~396 MB but the **transient worst case during an encode approaches ~1 GB** (frozen snapshot copy + sliced window + encoder buffers coexist), so soak with a real triggered encode, not just idle buffering. Only then flip `capture.async_encode` → `true` in edge_glass `config.yaml`.
   - Exit criteria: field deployment unblocked, pump sensors field-enabled, non-blocking capture live — the entire hardware backlog closed in one day.
3. ~~Theme 6 silent detector-blinding~~ ✅ **DONE** (`22e6084`+`3c110a2`). ~~Theme 4 retention/pruning~~ ✅ **DONE** (`dddda76`). ~~Theme 2 auth-hardening (bulk)~~ ✅ **DONE** (`c4660d8`). ~~Theme 5 request-storm + WS HOL + offline TOCTOU + N+1 + glass-LWT~~ ✅ **DONE** (`9d3f063`,`525452e`,`461c0ed`). ~~Theme 3 glass 4xx-retry + MQTT-connect-at-boot~~ ✅ **DONE** (`73b5af0`). ~~Blocking MP4 encode + buffer-window redesign~~ ✅ **DESIGNED + IMPLEMENTED 2026-07-13** (proposal `docs/superpowers/specs/2026-07-13-glass-mp4-encode-buffer-redesign.md`; impl `34efdf6` with the recommended defaults, shipped dormant behind `capture.async_encode`). **Only remaining step: the §8 hardware-validation gate** (bench Pi 5 + camera — confirm clip window, loop-never-blocks, second-event-detected, snapshot-doesn't-freeze, memory soak) before flipping the flag on in the field. After that, remaining items are cloud-gated (T1 PG 500s) or product/safety decisions (single-sensor fallback) or the hardware pump bench (spec §6).
4. **Theme 2 remainder** — bulk auth-hardening shipped (`c4660d8`); ✅ snapshot GET auth'd (`e73718d`). What's left is the heavier/coupled work: **per-node** API keys (registry + edge coordination, breaks single-key deploy), storage-endpoint auth + storage-path traversal review, WS-session tightening.
5. ~~Theme 5 remainder — live node-health WS push~~ ✅ **DONE** (`e73718d`) — instant-ONLINE push shipped, dead `broadcast_node_status` deleted; with glass LWT (`461c0ed`) both transitions push live. **Theme 5 complete.**
6. ~~Gate any PostgreSQL/cloud cutover on Theme 1~~ ✅ **Request paths PG-safe** (`e73718d`). Before a real cloud cutover, still do: a live-PG smoke test (dispatch is unit-tested only) + retention `datetime()` PG-portability (T4).
7. **Housekeeping:** normalize the test harness (edge_glass imports → one repo-root `pytest`). (~~migrate `datetime.utcnow()`~~ ✅ done `3a61fef`.)

---

*Method: directory + LOC census, live `pytest` runs, `git` divergence analysis, direct source re-verification, and a 9-subagent final whole-branch review + file-disjoint parallel-subagent slices (T2 security ×2, T6 glass ×5, detector-health ×2, T4 lifecycle ×2, T2 auth-hardening ×5, T5 observability ×3, T3 glass-autonomy ×2, T5 batch-cycles ×2, T5 glass-LWT ×2, non-blocking-capture ×2, utcnow-migration ×3, PG-safe server slice ×3, docs reconstruction ×4 + PM coverage audit). Findings marked ✓ were confirmed by reading current code.*
